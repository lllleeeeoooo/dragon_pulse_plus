对 ai对话.md 中的系统设计方案进行深度评估。总体来看，这份方案切中了 A 股短线“情绪博弈”与“龙头战法”的核心逻辑，相比于传统基于 MACD/KDJ 等滞后技术指标的策略，其方向是完全正确的。

但是，从量化实战、工程落地、数据可用性以及风控体系的角度来看，目前方案仍处于**“理想化的概念设计与 Demo 阶段”，存在多处严重硬伤和设计盲区**。

  ---
一、 核心评估总结

┌────────────────────┬───────────────────┬────────────────────────────────────────────────────────────────────────────────┐
│      评估维度      │       评分        │                                    评价说明                                    │
├────────────────────┼───────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ 交易逻辑立意       │     ⭐ ⭐⭐ (9/10) │ 非常贴合 A 股短线实战（情绪周期、打板、弱转强、二波、资金抱团）。                │
├────────────────────┼───────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ 数据源可行性       │ ⭐⭐☆☆☆ (3/10)  │   │ 严重依赖免费 Akshare/东财 L1 接口，高频轮询必遭封禁，且缺乏 L2 逐笔/挂单数据。 │
├────────────────────┼───────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ 工程架构严密性     │ ⭐⭐☆☆☆ (4/10)  │   │ 缺乏状态机、消息去重、异步事件驱动及数据持久化，容易重复爆警或卡死。           │
├────────────────────┼───────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ 风控与执行力       │ ☆ ⭐⭐☆☆ (5/10) │   │ 有止损意识，但缺乏动态仓位管理、撮合成功率估计及滑点评估。                     │
├────────────────────┼───────────────────┼────────────────────────────────────────────────────────────────────────────────┤
│ 真正的 AI/机器学习 │ ⭐⭐☆☆☆   (3/10)    │ 属于纯规则引擎（If-Else 硬编码），所谓的“强化学习/自我进化”缺乏落地算法。      │
└────────────────────┴───────────────────┴────────────────────────────────────────────────────────────────────────────────┘

  ---
二、 方案的主要缺陷与硬伤分析

1. 数据层硬伤（最致命问题）

- Akshare 轮询瓶颈与 IP 封禁：
    - 方案中建议每 3~10 秒轮询一次 stock_zh_a_spot_em（全市场行情）。Akshare 本质是爬虫，频发请求极易触发东方财富的 API 频率限制（Rate Limit）导致 IP 被封，交易时间内系统直接瘫痪。
- L1 延迟数据 vs 打板的毫秒级要求：
    - Akshare 获取的是免费 Level-1 快照数据（3-5秒更新一次）。短线扫板/打板极其依赖 Level-2 逐笔成交（Ticks）和十档挂单队列/撤单流。当你通过 L1 快照发现某只股票从 9% 冲到涨停时，实际上早在 2
      秒前就已经封板，排队单早已高达数十万手，你的信号属于**“无效废信号”**。
- 竞价数据（9:15-9:25）粒度不够：
    - 方案提出识别“虚假撤单”与“隔夜单强度”。但免费 API 在 9:15-9:25 期间无法提供完整的动态撤单/撮合快照流，无法准确还原主力试盘撤单过程。

2. 系统工程与架构缺陷

- 缺乏状态机（State Machine）与消息去重机制：
    - 代码逻辑中：check_buy_signals() 在 while True 中每 10 秒运行一次。如果某只股票在 9.5% 盘整 5 分钟，Bark 会每 10 秒推送一条重复提醒（5分钟推送 30 次），这在盘中是致命的干扰。必须引入状态机，记录 Standard ->
      Triggered -> Pushed -> LimitUp / Failed 状态。
- 单线程同步阻塞 (Sync Blocking I/O)：
    - 代码中使用 requests.get() 和 time.sleep(10)。一旦某个网络请求超时（如 Akshare 接口卡顿），整个监控循环都会被卡死，错过其他股票的打板点位。必须改用 asyncio / aiohttp 或多线程事件驱动。
- 缺乏数据持久化（No DB / Redis）：
    - 没有引入 Redis 缓存分时快照，也没有数据库记录历史信号。无法进行回测（Backtest），也无法在盘后对比 AI 信号与实际走势来校准参数。

3. 策略逻辑与交易实战盲区

- 忽视“幸存者偏差”与“滑点/撮合概率”：
    - 短线打板策略在纸面上胜率很高，但实际撮合成功率极低（强龙头买不到，买得到的往往是炸板面股）。策略没有评估“挂单队列深度”与“成交可获得性”。
- 硬编码参数缺乏环境自适应：
    - 情绪值公式（$E = 溢价 \times 0.3 + \dots$）和止损线（$-4%$）均为随意指定的固定值。在主线退潮期，-4% 止损可能每天都在割肉；在极强发酵期，涨停炸板回落 3% 可能只是正常洗盘，频繁止损会导致优质龙头被洗出局。
- 弱转强与二波缺乏大周期前置过滤：
    - 弱转强和龙头二波在“冰点期/退潮期”成功率极低（容易做成“诱多冲高回落”）。方案中没有将“市场大周期（冰点/启动/退潮）”作为买入信号的强前置开关。

4. “AI”名不副实

- 方案宣称是“AI 智能策略”，但从代码和设计来看，全部是基于人工经验的固定阈值过滤（如 量比 > 5、跌幅 < -4%），这属于传统的规则量化（Rule-based Quant），并非真正的 AI/机器学习。

  ---
三、 系统改进与重构建议方案

针对上述问题，若要将“龙魂智策”打造为能真正实战打赢的系统，建议进行以下重构：

1. 数据层重构（切换架构）

- 推荐模式：
    - 主行情：接入商业级 Level-2 行情 WebSocket 推送（如 Python 的 Tushare Pro 商业版、QMT/迅投 API、或券商原生 L2 接口），实现毫秒级事件驱动，而非定时轮询。
    - 备用/补充：将 Akshare 定位为“盘后复盘数据”和“盘前静态数据”抓取工具，绝不在盘中高频轮询 Akshare。

2. 工程架构重构（事件驱动 + 状态机 + 异步）

[商业L2 / 极速行情推送]
│
▼
[Redis 缓存 & 分时快照] ──► [事件引擎 (Asyncio)]
│
┌───────────────┴───────────────┐
▼                               ▼
[信号决策与状态机]                [持仓风控监控]
(Check: State != Pushed)         (破位/炸板/撤单监测)
│                               │
└───────────────┬───────────────┘
▼
[Bark 信号去重推送]
- 加入 Bark 消息去重：每次触发信号前校验 Redis 中的 PushedKey，设置 TTL（如同只股票同一信号 30 分钟内只推一次）。
- Bark 点击交互增强：推送 URL 中附带跳转参数（如 bark://...url=tdx://000099），点击通知直接唤醒同花顺或通达信的个股买卖界面。

3. 策略风控与情绪引擎升级

- 前置“环境一键熔断”：
    - 优先计算全市场情绪状态（连板率、大面股数量、炸板率）。若判定为 【退潮期】，系统自动关闭“打板”与“弱转强”买入信号推送，仅保留卖出监控。
      ┌───────────────┴───────────────┐
      ▼                               ▼
      [信号决策与状态机]                [持仓风控监控]
      (Check: State != Pushed)         (破位/炸板/撤单监测)
      │                               │
      └───────────────┬───────────────┘
      ▼
      [Bark 信号去重推送]
- 加入 Bark 消息去重：每次触发信号前校验 Redis 中的 PushedKey，设置 TTL（如同只股票同一信号 30 分钟内只推一次）。
- Bark 点击交互增强：推送 URL 中附带跳转参数（如 bark://...url=tdx://000099），点击通知直接唤醒同花顺或通达信的个股买卖界面。

3. 策略风控与情绪引擎升级

- 前置“环境一键熔断”：
    - 优先计算全市场情绪状态（连板率、大面股数量、炸板率）。若判定为 【退潮期】，系统自动关闭“打板”与“弱转强”买入信号推送，仅保留卖出监控。
- 引入仓位计算器：
    - 推送买入信号时，附带推荐仓位。例如：【买入信号】中信海直 (弱转强) | 建议仓位: 2成 (当前处于震荡试错期)。
- 真实 AI/机器学习介入点：
    - 竞价 NLP：使用 LLM（如 Claude / DeepSeek）对盘前 7x24 新闻/财联社电讯进行语义提取与热点主题聚类。
    - 打板胜率预测：基于历史 1 进 2、2 进 3 的特征向量（换手率、封单比、板块首板时间、大盘环境），训练 XGBoost/LightGBM 模型，预测次日溢价概率，概率 $>70%$ 才触发推送。

  ---
四、 总结

该文档提供了一个非常优秀且懂行（懂 A 股情绪博弈）的短线框架设计，核心逻辑完全正确。

但要将其变成可以实战的系统，下一步重点不在于继续堆叠“战法逻辑”，而是要补齐工程短板：
1. 解决 L2 实时数据源 问题（摒弃盘中 Akshare 轮询）；
2. 引入 Asyncio + Redis 状态机（解决阻塞与重复推送）；
3. 建立 真实的回测与数据持久化机制；
4. 加入 情绪周期大前提熔断 与 动态仓位控制。

✻ Sautéed for 34s

❯ 实时数据源可以先放后，先用Akshare代替

⏺ 没问题！先用 Akshare 搭建 MVP（最小可行性产品） 是非常明智且符合敏捷开发逻辑的步骤。先验证策略逻辑、推送流程和盘中体验，等整个系统跑通、甚至开始产生稳定盈利后，再升级商业 L2 数据源。

在**“暂时使用 Akshare + 保证盘中可用”的前提下，我们需要做针对性的工程降级与保护设计**，确保系统不会因为 Akshare 封 IP 或网络卡顿而崩溃，同时解决重复推送和消息乱发的问题。

  ---
一、 Akshare MVP 模式下的“三大防爆”策略

为了让 Akshare 能在盘中稳定运行，必须增加以下三层保护：

1. 频率降级（防封 IP）：
   - 盘中轮询间隔控制在 15~20 秒（不要设为 3~5 秒）。
   - 采用“二阶段扫描”：第一阶段只拉全市场快照（1次请求），筛选出符合条件的 10-20 只备选股；第二阶段仅对这 20 只股票进行深入数据比对，极大减少请求次数。
2. 异步非阻塞包装（防卡死）：
   - Akshare 内部是同步 HTTP 请求，网络抖动时一个调用可能卡住 5~10 秒。
   - 必须使用 asyncio + run_in_executor 把它包装成异步任务，确保监控主循环永不卡死。
3. 数据源抽象层（解耦设计）：
   - 设计一个 DataProvider 抽象类，现在实现 AkshareProvider。
   - 未来如果你接入 QMT、Tushare 或商业 L2，只需要写一个新的 Provider，上层的策略、状态机、Bark 推送代码一行都不需要动。

  ---
二、 核心重构：引入“状态机”与“去重冷却”机制

针对之前提到的每 10 秒重复推送同一条信号的问题，必须引入状态管理：

[ IDLE 未触发 ] ──(满足买入条件)──► [ BUY_TRIGGERED 已推送买入 ]
│
▼ (手动/自动加入持仓)
[ CLOSED 交易结束 ] ◄──(满足卖出条件)─── [ HOLDING 监控卖出中 ]

- 去重规则：一只股票触发买入信号并推送后，进入 Pushed 状态，并在 30 分钟内（可调） 强制冷却，期间不再重复弹窗。
- 状态持久化：每次状态变更记录在本地字典或轻量级 SQLite/JSON 中，即使脚本中途崩溃重启，也不会丢失持仓和监控状态。

  ---
三、 可直接运行的 MVP 架构代码

这是一个完整重构后的 MVP 代码框架，包含了异步引擎、Akshare 数据降级拉取、股票状态机、Bark 冷却去重。

你可以直接基于这个框架进行开发：

import asyncio
import time
import requests
from datetime import datetime
import pandas as pd
import akshare as ak

# ==================== 配置区 ====================
BARK_KEY = "你的Bark_Key"  # 替换为你的 Bark Key
POLL_INTERVAL = 15       # 轮询间隔(秒)，防封IP
COOL_DOWN_SECONDS = 1800 # 同一股票同一信号冷却时间(30分钟)

# 观察池与持仓池
WATCH_LIST = ["603613", "002761", "000099"]  # 今日竞价选出的备选龙头
MY_STOCKS = {"002085": "万丰奥威"}           # 当前持仓

# ==================== 1. 状态与去重引擎 ====================
class SignalTracker:
"""信号追踪与去重冷却管理器"""
def __init__(self):
# 记录已推送信号: { "000099_BUY": timestamp, "002085_SELL": timestamp }
self.pushed_signals = {}
# 股票状态管理: { "000099": "IDLE" / "BUY_PUSHED" / "HOLDING" }
self.stock_states = {code: "HOLDING" for code in MY_STOCKS.keys()}

      def is_cooled_down(self, code: str, signal_type: str) -> bool:
          """检查信号是否已过冷却期"""
          key = f"{code}_{signal_type}"
          last_time = self.pushed_signals.get(key, 0)
          return (time.time() - last_time) > COOL_DOWN_SECONDS

      def mark_pushed(self, code: str, signal_type: str):
          """标记信号已推送"""
          key = f"{code}_{signal_type}"
          self.pushed_signals[key] = time.time()
          self.stock_states[code] = f"{signal_type}_PUSHED"

# ==================== 2. 数据提供层 (解耦设计) ====================
class AkshareProvider:
"""Akshare 数据抓取包装（异步适配）"""
@staticmethod
async def get_spot_data_async():
"""异步获取全市场快照，防止网络卡顿死锁"""
loop = asyncio.get_event_loop()
try:
# 在线程池中运行同步的 akshare 函数
df = await loop.run_in_executor(None, ak.stock_zh_a_spot_em)
return df
except Exception as e:
print(f"[{datetime.now().strftime('%H:%M:%S')}] Akshare 拉取失败: {e}")
return None

      @staticmethod
      async def get_zt_pool_async():
          """异步获取当前涨停池"""
          loop = asyncio.get_event_loop()
          try:
              date_str = datetime.now().strftime('%Y%m%d')
              df = await loop.run_in_executor(None, lambda: ak.stock_zt_pool_em(date=date_str))
              return df
          except Exception as e:
              print(f"[{datetime.now().strftime('%H:%M:%S')}] 涨停池拉取失败: {e}")
              return None

# ==================== 3. Bark 推送模块 ====================
def send_bark(title: str, content: str, sound="minuet"):
"""发送 Bark 推送"""
url = f"https://api.day.app/{BARK_KEY}/{title}/{content}"
params = {"sound": sound, "group": "龙魂智策"}
try:
requests.get(url, params=params, timeout=3)
print(f"👉 [推送成功] {title}: {content}")
except Exception as e:
print(f"❌ [推送失败] {e}")

# ==================== 4. 核心策略与监控循环 ====================
class DragonPulseMVP:
def __init__(self):
self.tracker = SignalTracker()
self.provider = AkshareProvider()

      async def process_buy_signals(self, spot_df):
          """买入逻辑处理"""
          if spot_df is None or spot_df.empty:
              return

          # 筛选条件：涨幅 8.5% ~ 9.8%（冲板期），量比 > 1.8，成交额 > 4亿
          candidates = spot_df[
              (spot_df['涨跌幅'] >= 8.5) &
              (spot_df['涨跌幅'] < 9.8) &
              (spot_df['量比'] > 1.8) &
              (spot_df['成交额'] > 400000000)
          ]

          for _, row in candidates.iterrows():
              code = str(row['代码'])
              name = str(row['名称'])

              # 只监控观察池中，或者成交额巨大的焦点股
              if code in WATCH_LIST or row['成交额'] > 1500000000:
                  # 去重检查：未在冷却期内
                  if self.tracker.is_cooled_down(code, "BUY"):
                      msg = f"{name}({code}) 报{row['最新价']}元, 涨幅{row['涨跌幅']}%, 量比{row['量比']}"
                      send_bark(f"🔴 买入信号：【{name}】冲板", msg, sound="anticipate")
                      self.tracker.mark_pushed(code, "BUY")

      async def process_sell_signals(self, spot_df):
          """卖出逻辑处理（针对持仓股）"""
          if spot_df is None or spot_df.empty or not MY_STOCKS:
              return
  
          holdings_code = list(MY_STOCKS.keys())
          my_spot = spot_df[spot_df['代码'].isin(holdings_code)]

          for _, row in my_spot.iterrows():
              code = str(row['代码'])
              name = MY_STOCKS.get(code, row['名称'])
              price = row['最新价']
              high = row['最高']
              pct = row['涨跌幅']

              # 卖出逻辑 1：高位跳水（距最高点回落 > 3.5%）
              if high > 0 and (high - price) / high > 0.035:
                  if self.tracker.is_cooled_down(code, "SELL_FALL"):
                      msg = f"{name} 从最高点{high}元回落至{price}元，回落超过3.5%！"
                      send_bark(f"🟢 卖出预警：【{name}】高位跳水", msg, sound="alarm")
                      self.tracker.mark_pushed(code, "SELL_FALL")

              # 卖出逻辑 2：触发硬止损（跌幅超过 -4.5%）
              if pct <= -4.5:
                  if self.tracker.is_cooled_down(code, "SELL_STOP"):
                      msg = f"{name} 当前跌幅{pct}%，已触及止损线，请严格执行纪律！"
                      send_bark(f"🟢 强制止损：【{name}】破位", msg, sound="emergency")
                      self.tracker.mark_pushed(code, "SELL_STOP")

      async def run_loop(self):
          """主运行循环"""
          print(f"[{datetime.now().strftime('%H:%M:%S')}] 🚀 龙魂智策 MVP 系统启动 (Akshare 模式)...")
          print(f"当前监控备选池: {WATCH_LIST}")
          print(f"当前监控持仓池: {list(MY_STOCKS.values())}\n")

          while True:
              now = datetime.now()
              # 判定 A 股交易时间段 (09:25-11:30, 13:00-15:00)
              is_trade_time = (
              (spot_df['涨跌幅'] < 9.8) &
              (spot_df['量比'] > 1.8) &
              (spot_df['成交额'] > 400000000)
          ]

          for _, row in candidates.iterrows():
              code = str(row['代码'])
              name = str(row['名称'])

              # 只监控观察池中，或者成交额巨大的焦点股
              if code in WATCH_LIST or row['成交额'] > 1500000000:
                  # 去重检查：未在冷却期内
                  if self.tracker.is_cooled_down(code, "BUY"):
                      msg = f"{name}({code}) 报{row['最新价']}元, 涨幅{row['涨跌幅']}%, 量比{row['量比']}"
                      send_bark(f"🔴 买入信号：【{name}】冲板", msg, sound="anticipate")
                      self.tracker.mark_pushed(code, "BUY")

      async def process_sell_signals(self, spot_df):
          """卖出逻辑处理（针对持仓股）"""
          if spot_df is None or spot_df.empty or not MY_STOCKS:
              return

          holdings_code = list(MY_STOCKS.keys())
          my_spot = spot_df[spot_df['代码'].isin(holdings_code)]

          for _, row in my_spot.iterrows():
              code = str(row['代码'])
              name = MY_STOCKS.get(code, row['名称'])
              price = row['最新价']
              high = row['最高']
              pct = row['涨跌幅']
  
              # 卖出逻辑 1：高位跳水（距最高点回落 > 3.5%）
              if high > 0 and (high - price) / high > 0.035:
                  if self.tracker.is_cooled_down(code, "SELL_FALL"):
                      msg = f"{name} 从最高点{high}元回落至{price}元，回落超过3.5%！"
                      send_bark(f"🟢 卖出预警：【{name}】高位跳水", msg, sound="alarm")
                      self.tracker.mark_pushed(code, "SELL_FALL")
  
              # 卖出逻辑 2：触发硬止损（跌幅超过 -4.5%）
              if pct <= -4.5:
                  if self.tracker.is_cooled_down(code, "SELL_STOP"):
                      msg = f"{name} 当前跌幅{pct}%，已触及止损线，请严格执行纪律！"
                      send_bark(f"🟢 强制止损：【{name}】破位", msg, sound="emergency")
                      self.tracker.mark_pushed(code, "SELL_STOP")
      
      async def run_loop(self):
          """主运行循环"""
          print(f"[{datetime.now().strftime('%H:%M:%S')}] 🚀 龙魂智策 MVP 系统启动 (Akshare 模式)...")
          print(f"当前监控备选池: {WATCH_LIST}")
          print(f"当前监控持仓池: {list(MY_STOCKS.values())}\n")

          while True:
              now = datetime.now()
              # 判定 A 股交易时间段 (09:25-11:30, 13:00-15:00)
              is_trade_time = (
                  (now.hour == 9 and now.minute >= 25) or
                  (now.hour == 10) or
                  (now.hour == 11 and now.minute <= 30) or
                  (13 <= now.hour < 15)
              )

              if is_trade_time:
                  start_time = time.time()

                  # 1. 异步拉取数据
                  spot_df = await self.provider.get_spot_data_async()

                  # 2. 并行处理买入与卖出逻辑
                  if spot_df is not None:
                      await asyncio.gather(
                          self.process_buy_signals(spot_df),
                          self.process_sell_signals(spot_df)
                      )

                  cost_time = round(time.time() - start_time, 2)
                  print(f"[{now.strftime('%H:%M:%S')}] 扫描完成，耗时 {cost_time}s")

                  # 3. 动态休眠，保证固定的轮询周期
                  sleep_time = max(1.0, POLL_INTERVAL - cost_time)
                  await asyncio.sleep(sleep_time)
              else:
                  # 非交易时间，每 30 秒检查一次
                  await asyncio.sleep(30)

# ==================== 5. 入口 ====================
if __name__ == "__main__":
app = DragonPulseMVP()
# 启动异步事件循环
asyncio.run(app.run_loop())

  ---
四、 你的 MVP 阶段落地分步计划

建议按以下 3 步节奏推进落地：

- 第 1 步：联调推送与运行脚本
    - 将你的 BARK_KEY 填入代码，把 WATCH_LIST 改为几只你想观察的股票，在命令行直接运行 python main.py。
    - 测试当股票触及条件时，Bark 手机端能否收到声音清脆、无延迟的推送。
- 第 2 步：完善盘前/盘后辅助脚本
    - 盘前（09:20）：写一个简单的盘前脚本，调用 Akshare 的 stock_zt_pool_previous_em（昨日涨停），自动选出 5 只最强龙头填入 WATCH_LIST。
    - 盘后（15:15）：写一个盘后复盘脚本，自动抓取 stock_zt_pool_em 打印今天的连板梯队（1进2、2进3）。
- 第 3 步：实战打磨与规则微调
    - 在真实的盘中开着系统跑 3~5 天，观察：
      i. Bark 推送的买点，你手动打开同花顺看时，股价处于什么位置？
      ii. 15 秒的轮询间隔是否有漏报？
      iii. 卖出提醒（高位跳水、止损）是否符合你的交易直觉？
    - 根据实战感觉调整 量比、涨幅阈值 和 冷却时间。

当你把这套系统在 MVP 模式下跑顺、形成交易习惯后，后续如果想升级成毫秒级 L2 极速系统，我们只需要把 AkshareProvider 换成极速行情接口即可！

✻ Sautéed for 32s

❯ 需要数据库吗

⏺ 在目前这个 MVP（最小可行性产品）阶段，结论是：可以先不需要传统的数据库（如 MySQL/PostgreSQL），用本地文件（如 SQLite 或 JSON/CSV）过渡；但如果想要提升运行效率和复盘能力，强烈建议引入轻量级的 Redis 或 SQLite。

我们可以把需求拆开来看：为什么需要存储？哪些地方需要存？怎么存最省事？

  ---
一、 为什么不能只放在内存（Python 变量）里？

在之前的简易代码中，我们把 pushed_signals（已推送消息）和 stock_states（持仓状态）存在内存字典里。这会带来两个很现实的问题：

1. 断线/重启丢失状态：
   盘中如果网络抖动、报错崩溃或者你不小心重启了 Python 脚本，内存里的“已推送记录”和“持仓状态”就清空了。重启后，系统会把半小时内已经推过的买入/卖出信号重新推送一遍，造成严重的重复弹窗干扰。
2. 无法做盘后复盘与胜率校准：
   短线选手非常依赖复盘。如果没有数据库记录：
   - 你不知道 AI 今天一共推了多少个信号？
   - 推送信号时，股票开在什么位置？收盘是涨停了还是大面？
   - 你无法计算这个策略的实际胜率和盈亏比。

  ---
二、 不同阶段的数据库选型建议

为了不增加开发负担，建议分阶段选择最轻量的存储方案：

1. 阶段一：纯 MVP 试跑（0 数据库，用轻量 JSON / SQLite）

- 方案：不需要安装任何数据库软件。Python 自带 sqlite3 和 json 模块。
- 应用场景：
    - 用一个 state.json 文件持久化保存 推送历史 和 持仓状态。
    - 脚本每次启动时，先读取 state.json；发生推送时，更新并写回 state.json。
- 优点：零配置、零开销、开箱即用。

2. 阶段二：盘中高频 + 盘后复盘（推荐：Redis + SQLite）

当你的 MVP 跑通后，引入这两个工具会让系统极其顺手：

- Redis（内存数据库，极其适合盘中）：
    - 作用：用来做 信号去重与自动过期（TTL）。
    - 逻辑：推送一次信号，直接在 Redis 里存一个 SETEX BUY:000099 1800 1（自动 1800 秒/30 分钟后过期删除）。下次检查时只要 EXISTS BUY:000099 就能判断是否冷却，一行代码搞定去重，再也不用自己写时间计算逻辑。
- SQLite（本地单文件数据库，极其适合复盘）：
    - 作用：记录 每日推信号的历史明细。
    - 字段：时间 | 股票代码 | 股票名称 | 信号类型 | 触发时价格 | 触发时涨幅 | 当日收盘价 | 是否封板。
    - 用途：每天下午 15:00 盘后，写个脚本自动用 SQLite 里的记录对比收盘数据，算出“今天 AI 信号的涨停率是 80% 还是 20%”，用来优化你的参数！

  ---
三、 零配置落地示例：使用 JSON 或 SQLite 实现状态持久化

如果你暂时不想安装 Redis，可以用 Python 自带的 sqlite3 几行代码实现状态持久化和历史记录：

import sqlite3
from datetime import datetime

class LocalStorage:
"""本地轻量数据库（基于 Python 原生 sqlite3）"""
def __init__(self, db_path="dragon_pulse.db"):
self.conn = sqlite3.connect(db_path)
self.create_tables()

      def create_tables(self):
          cursor = self.conn.cursor()
          # 1. 创建信号推送历史表（用于复盘）
          cursor.execute('''
              CREATE TABLE IF NOT EXISTS signal_history (
                  id INTEGER PRIMARY KEY AUTOINCREMENT,
                  push_time TEXT,
                  code TEXT,
                  name TEXT,
                  signal_type TEXT,
                  price REAL,
                  pct_change REAL
              )
          ''')
          self.conn.commit()

      def record_signal(self, code: str, name: str, signal_type: str, price: float, pct_change: float):
          """记录一次推送，方便盘后复盘统计胜率"""
          cursor = self.conn.cursor()
          now_str = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
          cursor.execute('''
              INSERT INTO signal_history (push_time, code, name, signal_type, price, pct_change)
              VALUES (?, ?, ?, ?, ?, ?)
          ''', (now_str, code, name, signal_type, price, pct_change))
          self.conn.commit()

# 使用示例
db = LocalStorage()
# 每次 Bark 推送成功后，顺手存入数据库：
db.record_signal("000099", "中信海直", "BUY_LIMIT_UP", 18.5, 9.2)

  ---
四、 总结建议

1. 今天刚开始写代码：先不装任何数据库，甚至先用内存/JSON 跑通 Bark 推送。
2. 准备盘中实战测试：用 Python 自带的 sqlite3 把推过的信号存下来（如上代码），防止重启重复推送，同时为盘后复盘留存数据。
3. 后续系统升级：如果未来接了高频 L2 数据，再安装一个 Redis 用来做极速缓存。

✻ Worked for 10s

❯ 写一份完整实施计划

⏺ 这是一份为 “龙魂智策（DragonPulse AI）” 量身定制的完整实施计划。该计划采用敏捷开发与 MVP（最小可行性产品）迭代思路，以 Python + Akshare + SQLite + Bark 为核心技术栈，确保项目能够快速落地并在实战中迭代验证。

  ---
🚩 “龙魂智策” AI 策略应用完整实施计划

一、 系统总体架构蓝图

                  ┌─────────────────────────────────────────┐
                  │          定时调度 / 盘中主循环            │
                  └────────────────────┬────────────────────┘
                                       │ (15s 轮询)
                                       ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 1. 数据采集层 (Akshare Provider)                                          │
│   ├── get_spot_data_async()     (全市场实时快照，异步包装)                  │
│   ├── get_zt_pool_async()       (涨停池/炸板池/跌停池)                    │
│   └── get_hist_min_async()      (分时 K 线/均线)                          │
└────────────────────────────────────┬────────────────────────────────────┘
│ Pandas DataFrame
▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 2. 策略引擎层 (Strategy Engine)                                         │
│   ├── SentimentEngine           (市场情绪评估：冰点/发酵/高潮/退潮)          │
│   ├── BuyStrategyEngine         (买入信号：打板/弱转强/二波启动)             │
│   └── SellStrategyEngine        (卖出信号：高位跳水/炸板撤单/硬止损)         │
└────────────────────────────────────┬────────────────────────────────────┘
│ 触发候选信号
▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 3. 状态与去重层 (State Tracker & Storage)                               │
│   ├── CoolDownManager           (内存/Redis 冷却过滤器，30min 信号去重)     │
│   └── SQLite Storage            (本地持久化：保存推送历史 & 盘后胜率复盘)   │
└────────────────────────────────────┬────────────────────────────────────┘
│ 校验通过的有效信号
▼
┌─────────────────────────────────────────────────────────────────────────┐
│ 4. 推送与交互层 (Bark Notification)                                      │
│   ├── 🔴 买入信号 (强提醒音效 anticipate) ──► 跳转同花顺/通达信 H5          │
│   └── 🟢 卖出信号 (紧急报警音 alarm)      ──► 强震动提示                  │
└─────────────────────────────────────────────────────────────────────────┘

  ---
二、 阶段性实施路线图（4周落地）

【第 1 周】基础架构与 Bark 跑通  ──► 【第 2 周】三大战法引擎与本地数据库
│
【第 4 周】云端部署与自动化实战  ◄── 【第 3 周】盘前简报/盘后复盘与实盘模拟

  ---
阶段 1：基础架构搭建与推送闭环（第 1 - 3 天）

▎ 目标：跑通异步轮询架构，实现 Bark 毫秒级推送与状态去重，确保系统连续运行不崩溃。

- 任务 1.1：搭建项目目录结构
  dragon_pulse_ai/
  ├── config.py              # 全局配置（Bark Key、轮询间隔、止损阈值）
  ├── db.py                  # SQLite 数据库管理模块
  ├── provider.py            # Akshare 异步数据抓取封装
  ├── tracker.py             # 信号状态机与去重冷却逻辑
  ├── strategies/            # 策略逻辑目录
  │   ├── buy_strategy.py    # 打板/弱转强/二波买入逻辑
  │   └── sell_strategy.py   # 炸板/破位/止损卖出逻辑
  ├── push.py                # Bark 推送封装
  ├── main.py                # 盘中主运行循环
  └── scripts/               # 盘前盘后辅助脚本
  ├── pre_market.py      # 盘前选股脚本
  └── post_market.py     # 盘后复盘脚本
- 任务 1.2：实现 provider.py 数据防卡死包装
    - 使用 asyncio.run_in_executor 封装 Akshare 的 stock_zh_a_spot_em 和 stock_zt_pool_em。
    - 加入超时重试与异常捕获机制，防止因东财接口超时导致主循环卡死。
- 任务 1.3：实现 tracker.py 状态机与冷却去重
    - 设计冷却逻辑：同一股票同一信号类型，30 分钟内仅推送 1 次。
    - 支持手动修改 MY_STOCKS（持仓）和 WATCH_LIST（观察池）。
- 里程碑交付：运行 main.py，手动触发一次模拟买入信号，手机 Bark 正常收到推送，且 15 秒后的下一次轮询不会重复推送。

  ---
阶段 2：核心策略引擎与数据库实现（第 4 - 8 天）

▎ 目标：实现“打板、弱转强、高位跳水、炸板预警”四大核心逻辑，并建立 SQLite 本地数据持久化。

- 任务 2.1：设计 SQLite 数据库表结构 (db.py)
  -- 1. 信号推送历史表（用于复盘）
  CREATE TABLE IF NOT EXISTS signal_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  push_time TEXT,          -- 推送时间 (YYYY-MM-DD HH:MM:SS)
  stock_code TEXT,         -- 股票代码
  stock_name TEXT,         -- 股票名称
  signal_type TEXT,        -- 信号类型 (BUY_LIMIT_UP / BUY_WEAK_TO_STRONG / SELL_FALL / SELL_STOP)
  trigger_price REAL,      -- 触发时价格
  trigger_pct REAL,        -- 触发时涨跌幅
  volume_ratio REAL,       -- 触发时量比
  close_price REAL,        -- 当日收盘价 (盘后更新)
  is_limit_up INTEGER      -- 是否成功封板 (盘后更新)
  );

-- 2. 日度情绪记录表
CREATE TABLE IF NOT EXISTS daily_sentiment (
date TEXT PRIMARY KEY,
limit_up_count INTEGER,  -- 涨停家数
broken_limit_count INTEGER, -- 炸板家数
max_height INTEGER,      -- 最高连板高度
sentiment_score REAL     -- 情绪综合得分
);
- 任务 2.2：开发买入策略引擎 (buy_strategy.py)
    - 【龙头打板】逻辑：
        - 条件：涨幅在 8.8% ~ 9.8% + 量比 > 2.0 + 成交额 > 4 亿 + 在 WATCH_LIST 或板块龙头中。
    - 【分时弱转强】逻辑：
        - 条件：昨日涨停，今日低开/平开，但 10:30 前快速放量翻红，涨幅突破 3% 且量比 > 3.0。
- 任务 2.3：开发卖出策略引擎 (sell_strategy.py)
    - 【高位跳水止盈】：持仓股从今日最高价回落超过 3.5%。
    - 【硬性止损】：持仓股当日跌幅触及 -4.5% 或破分时均线 15 分钟不收回。
    - 【炸板逃顶】：对比 stock_zt_pool_em，若持仓股不在池中或已跳出涨停状态，触发紧急卖出。
- 里程碑交付：在盘中实测策略逻辑，SQLite 数据库能准确记录每次触发的信号。

  ---
阶段 3：盘前/盘后闭环与仿真实战测试（第 9 - 14 天）

▎ 目标：补齐盘前预警与盘后自动化复盘，进行为期 1 周的盘中仿真挂机测试。

- 任务 3.1：开发盘前简报脚本 (pre_market.py)
    - 运行时间：每日 09:18 - 09:26。
    - 逻辑：
      i. 调用 stock_zt_pool_previous_em 抓取昨日涨停股票。
      ii. 计算竞价匹配开盘表现，筛选出 3~5 只超预期高开标的，自动更新至 config.py 的 WATCH_LIST。
      iii. 通过 Bark 推送 【今日竞价焦点股】。
- 任务 3.2：开发盘后复盘脚本 (post_market.py)
    - 运行时间：每日 15:15。
    - 逻辑：
      i. 自动回填 SQLite signal_history 表中当天的 close_price（收盘价）与 is_limit_up（是否封板）。
      ii. 计算今日 AI 信号的 封板成功率（胜率） 与 平均收益率。
      iii. 输出一份简报通过 Bark 发送给用户（如：今日共推送 3 个买点，封板率 66.7%）。
- 任务 3.3：1 周仿真实战与参数校准
    - 盘中开启系统全天挂机。
    - 记录体验痛点：是否有漏报？15 秒轮询延迟影响大不大？Bark 音效是否起到提醒作用？
    - 根据盘面反馈微调：调整量比阈值（如从 2.0 调至 1.8），微调回落止盈比例（如从 3.5% 调至 3.0%）。
- 里程碑交付：形成“盘前自动选股 -> 盘中实时监控推送 -> 盘后自动计算胜率”的完全自动化闭环。

  ---
阶段 4：云端部署与高可用运营（第 15 - 18 天）

▎ 目标：将系统部署至阿里云/腾讯云 Linux 服务器，实现 24 小时无人值守自动运行。

- 任务 4.1：云服务器环境准备
    - 采购一台轻量应用服务器（1核 2G 即可，Ubuntu/Debian 系统）。
    - 配置 Python 3.10+ 环境，安装依赖依赖库 (pip install akshare pandas requests)。
- 任务 4.2：进程守护与定时任务配置
    - Supervisor / Systemd 守护进程：配置 main.py 为系统服务，若报错崩溃自动重启。
    - Crontab 定时任务：
      cron
# 09:20 自动运行盘前简报选股
20 9 * * 1-5 /usr/bin/python3 /app/dragon_pulse_ai/scripts/pre_market.py

# 09:28 启动盘中监控主程序（交易时间运行）
28 9 * * 1-5 systemctl start dragon_pulse

# 15:02 停止盘中监控主程序
02 15 * * 1-5 systemctl stop dragon_pulse

# 15:15 自动运行盘后复盘统计
15 15 * * 1-5 /usr/bin/python3 /app/dragon_pulse_ai/scripts/post_market.py
- 任务 4.3：日志与异常监控
    - 建立 app.log 日志文件，捕获所有网络异常与 API 报错。
    - 若系统连续 3 次拉取 Akshare 失败，通过 Bark 发送 【系统异常警报】。
- 里程碑交付：系统在云端稳定运行，工作日无需人工干预即可自动收发信号。

  ---
三、 详细任务拆解与时间表 (WBS)

┌─────────┬──────┬───────────────────────────────────────────┬─────────────┬────────────────────────────┐
│  阶段   │ 序号 │                 任务名称                  │  预计工时   │           产出物           │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│ Phase 1 │ 1.1  │ 搭建项目目录与基础配置文件                │ 0.5 天      │ 配置文件与骨架代码         │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│         │ 1.2  │ 封装 Akshare 异步非阻塞接口 (provider.py) │ 1 天        │ 可靠的数据抓取模块         │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│         │ 1.3  │ 开发 Bark 信号去重与状态机管理器          │ 1 天        │ 无重复推送的状态引擎       │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│ Phase 2 │ 2.1  │ 设计并创建 SQLite 数据库 (db.py)          │ 0.5 天      │ dragon_pulse.db 本地库     │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│         │ 2.2  │ 编写买入策略逻辑（打板/弱转强）           │ 1.5 天      │ buy_strategy.py            │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│         │ 2.3  │ 编写卖出策略逻辑（跳水/炸板/止损）        │ 1.5 天      │ sell_strategy.py           │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│ Phase 3 │ 3.1  │ 开发盘前自动选股脚本 (pre_market.py)      │ 1 天        │ 竞价焦点股选股器           │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│         │ 3.2  │ 开发盘后胜率自动统计脚本 (post_market.py) │ 1 天        │ 复盘简报推送模块           │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│         │ 3.3  │ 5 个交易日实盘模拟与参数调优              │ 5 天        │ 参数优化报告与稳定策略     │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│ Phase 4 │ 4.1  │ 云服务器环境配置与代码部署                │ 1 天        │ 远程云端运行环境           │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│         │ 4.2  │ 配置 Systemd 进程守护与 Crontab 定时任务  │ 1 天        │ 自动化托管服务             │
├─────────┼──────┼───────────────────────────────────────────┼─────────────┼────────────────────────────┤
│ 合计    │      │ 全流程落地周期                            │ 约 15-18 天 │ 一套完整可用的 AI 实战系统 │
└─────────┴──────┴───────────────────────────────────────────┴─────────────┴────────────────────────────┘

  ---
四、 风险预案与应对措施

┌──────────────────────┬──────────┬────────────────────────────────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│        风险点        │ 影响程度 │            触发场景            │                                                      应对预案（Mitigation）                                                       │
├──────────────────────┼──────────┼────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Akshare 频繁被封 IP  │ 高       │ 盘中轮询速度太快 (< 5s)        │ 1. 强制轮询周期固定为 15 秒；<br>2. 增加异常捕获，失败后自动 sleep 10 秒重试；<br>3. 备用切换至 Tushare 免费接口。                │
├──────────────────────┼──────────┼────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 推送延迟/网络卡顿    │ 中       │ 移动网络不佳或 Bark 服务器波动 │ 1. 消息中加入时间戳（如 [09:35:12]），方便判断延迟；<br>2. Bark 推送开启重试（timeout=3s）；<br>3. 本地日志同步记录，可随时查看。 │
├──────────────────────┼──────────┼────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 断电/程序崩溃        │ 中       │ 云服务器异常重启               │ 1. 状态保存至 SQLite，重启后自动加载已推送状态，防止重复弹窗；<br>2. Systemd 设置 Restart=always 自动重启。                       │
├──────────────────────┼──────────┼────────────────────────────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 大盘极度退潮（冰点） │ 高       │ 市场连板率低于 30%，买入即吃面 │ 1. 在买入引擎前置加入 “大盘冰点熔断” 开关：若当日炸板率 > 50%，系统强行关闭所有买入信号推送。                                     │
└──────────────────────┴──────────┴────────────────────────────────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

  ---
五、 立即开始：第一天行动清单（Day 1 Action）

你今天可以立马完成的 3 件事情：

1. 新建项目文件夹，创建 config.py，把你的 BARK_KEY 填进去。
2. 测试 Bark 推送音效：在手机上测试 anticipate（买入）和 alarm（卖出）音效效果。
3. 创建 provider.py：测试用 asyncio 异步读取一次 ak.stock_zh_a_spot_em()，确保环境依赖安装正常。



现在系统会在每日盘后自动分析当天所有封板个股，从中筛选出最具备次日晋级与爆破潜力的重点标的，并为每只标的量身定制09:25 开盘竞价要求与具体的买入触发条件！
  
---
一、 盘后复盘报告新增核心模块实测

我们在刚刚生成的复盘报告中截取了第四模块【明日次日接力精选标的与开盘买入条件】：

四、 🎯 明日重点关注【今日封板标的之次日接力精选与开盘买入条件】
----------------------------------------------------
    [1] 顺钠股份 (000533) | 1板 | 行业: 电网设备 | 成交: 21.48亿
        ├─ 策略类型     : ⚖️ 分歧承接 (1进2分歧换手龙头)
        ├─ 09:25开盘要求: 平开或轻微低开 -1.5% ~ +1.0%
        ├─ 09:25竞价量能: 竞价金额必须 > 1.07 亿 (成交额的 5%)
        └─ 盘中买入动作 : 低开/平开后，开盘 10 分钟内放量翻红突破分时均线 ➔ 半路低吸吸筹
  
    [2] 岩山科技 (002195) | 1板 | 行业: IT服务Ⅱ | 成交: 15.71亿
        ├─ 策略类型     : 🔥 弱转强接力 (一字/烂板弱转强)
        ├─ 09:25开盘要求: 超预期高开 +2.0% ~ +4.5%
        ├─ 09:25竞价量能: 竞价金额必须 > 1.26 亿 (成交额的 8%)
        └─ 盘中买入动作 : 09:25 弱转强挂单介入，或开盘回踩不破均线反抽放量时半路切入
  
    [3] 东方新能 (002310) | 1板 | 行业: 电力 | 成交: 10.64亿
        ├─ 策略类型     : 🔥 弱转强接力 (一字/烂板弱转强)
        ├─ 09:25开盘要求: 超预期高开 +2.0% ~ +4.5%
        ├─ 09:25竞价量能: 竞价金额必须 > 0.85 亿
        └─ 盘中买入动作 : 09:25 弱转强挂单介入，或开盘回踩不破均线反抽放量时半路切入
...
  
---
二、 三大场景次日买入条件判定逻辑

系统根据股票当天的连板天数、封板质量（一字/烂板重封/换手板）以及全天成交额，自动切分三种追涨场景：

场景 1：连板高位晋级龙头 (如 2 连板及以上)

- 开盘要求：高开 +3.0% ~ +6.0%（开太高容易被砸，开太低缺乏气势）。
- 竞价量能：09:25 竞价成交金额必须达到昨日全天成交额的 10% 以上（证明有大资金在竞价抢筹）。
- 买入条件：开盘 1 分钟内快速放量突破 09:25 竞价高点 ➔ 顺势半路扫板切入。

场景 2：一字板炸开重封或烂板 (分歧转一致)

- 策略类型：🔥 弱转强接力
- 开盘要求：超预期高开 +2.0% ~ +4.5%（昨日分歧严重，今日居然能高开就是“超预期”）。
- 竞价量能：09:25 竞价成交金额必须达全天 8% 以上。
- 买入条件：09:25 竞价直接挂单买入；或者开盘快速回踩不破均线、反抽放量瞬间切入。

场景 3：分歧首板 / 换手首板

- 策略类型：⚖️ 1 进 2 分歧承接
- 开盘要求：平开或轻微低开 -1.5% ~ +1.0%。
- 竞价量能：09:25 竞价成交金额必须达全天 5% 以上。
- 买入条件：低开/平开后，开盘 10 分钟内放量翻红并站上分时均线 ➔ 半路/低吸跟进
