# 龙魂智策 (DragonPulse AI) — 系统设计评估与实施计划

## 一、 系统评估报告

### 1.1 核心优势
* **切中 A 股短线博弈本质**：不同于传统技术指标，本方案聚焦于“量化情绪”，涵盖打板、弱转强、龙头二波、资金抱团等实战战法。
* **清晰的交易生命周期**：涵盖盘前简报、盘中异动/买卖提醒、盘后复盘与自我进化。

### 1.2 存在的问题与硬伤
1. **数据源依赖与频控瓶颈**：
   - 依赖 Akshare/东财 L1 接口高频轮询，盘中易触发 API Rate Limit 导致 IP 被封。
   - L1 行情（3-5秒快照）缺乏 L2 逐笔成交与十档挂单队列，打板信号存在滞后风险。
2. **架构与消息去重缺陷**：
   - 缺少状态机（State Machine）与冷却去重机制，同一股票容易重复高频爆警。
   - 同步 HTTP 请求易在网络抖动时导致主循环卡死。
3. **缺少持久化与胜率校准**：
   - 无数据库记录历史信号，无法进行真实胜率与盈亏比回测。
4. **风控与环境适应性**：
   - 缺少市场大周期（冰点/发酵/退潮）的前置熔断机制。

### 1.3 改进优化方案
* **数据层**：MVP 阶段采用 Akshare 15 秒轮询 + 异步非阻塞包装；后期平滑升级为商业级 Level-2 / QMT 行情。
* **工程架构**：采用 `asyncio` 异步引擎 + SQLite 持久化 + Bark 冷却去重状态机。
* **策略风控**：加入盘前/盘后自动闭环与情绪冰点熔断机制。

---

## 二、 完整实施计划

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                 盘中主循环 (15s 轮询)                    │
└────────────────────────────┬────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────┐
│ 1. 数据采集层 (provider.py)                             │
│    - get_spot_data_async() [全市场快照]                 │
│    - get_zt_pool_async()   [涨停/炸板池]                │
└────────────────────────────┬────────────────────────────┘
                             │ Pandas DataFrame
                             ▼
┌─────────────────────────────────────────────────────────┐
│ 2. 策略引擎层 (strategies/)                             │
│    - BuyStrategy Engine  [打板/弱转强/二波]              │
│    - SellStrategy Engine [跳水/炸板/止损]              │
└────────────────────────────┬────────────────────────────┘
                             │ 触发候选信号
                             ▼
┌─────────────────────────────────────────────────────────┐
│ 3. 状态与去重层 (tracker.py & db.py)                    │
│    - SignalTracker [冷却去重 30min]                     │
│    - SQLite DB     [持久化与盘后复盘]                   │
└────────────────────────────┬────────────────────────────┘
                             │ 校验通过信号
                             ▼
┌─────────────────────────────────────────────────────────┐
│ 4. 推送交互层 (push.py)                                 │
│    - Bark Push [买入/卖出分级音效通知]                  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 实施阶段与时间表

| 阶段 | 周期 | 核心任务 | 交付物 |
| :--- | :--- | :--- | :--- |
| **阶段 1** | 第 1-3 天 | 项目骨架、异步数据抓取 (`provider.py`)、Bark 推送与信号冷却状态机 (`tracker.py`) | 基础通信与去重框架 |
| **阶段 2** | 第 4-8 天 | SQLite 数据库存储 (`db.py`)、买入/卖出策略引擎 (`strategies/`) | 策略执行与持久化引擎 |
| **阶段 3** | 第 9-14 天 | 盘前选股 (`pre_market.py`)、盘后自动化复盘 (`post_market.py`)、1周模拟实测 | 自动化闭环系统 |
| **阶段 4** | 第 15-18 天 | 云服务器部署、Systemd 守护进程与 Crontab 定时托管 | 云端无人值守系统 |

### 2.3 风险应对策略
* **API 封禁**：严格保持 15 秒轮询间隔，超时重试，异常时安全退避。
* **程序崩溃重启**：状态写入 SQLite/内存状态机，重启后自动加载，防重复弹窗。
* **极端退潮风险**：设置情绪熔断开关，全场炸板率过多时自动关闭买入信号。
