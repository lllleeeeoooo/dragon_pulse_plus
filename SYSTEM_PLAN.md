# dragon_pulse_plus AI智能情绪周期策略系统 - 开发与测试计划文档

---

# 目录
1. [系统整体架构图](#一系统整体架构图)
2. [开发计划（7天里程碑）](#二开发计划7天里程碑)
   - [阶段一：项目基础设施与数据采集模块（第 1-2 天）](#阶段一项目基础设施与数据采集模块第-1-2-天)
   - [阶段二：量化逻辑与情绪评分引擎（第 3-4 天）](#阶段二量化逻辑与情绪评分引擎第-3-4-天)
   - [阶段三：LLM 大模型决策引擎开发（第 5-6 天）](#阶段三llm-大模型决策引擎开发第-5-6-天)
   - [阶段四：定时调度与盘中 15 秒轮询监控系统（第 7 天）](#阶段四定时调度与盘中-15-秒轮询监控系统第-7-天)
3. [项目目录结构说明](#三项目目录结构说明)
4. [测试计划](#四测试计划)
   - [1. 单元测试 (Unit Testing)](#1-单元测试-unit-testing)
   - [2. 离线数据模拟测试 (Historic Replay / LLM Evaluation)](#2-离线数据模拟测试-historic-replay--llm-evaluation)
   - [3. 全链路集成测试 (Integration Testing)](#3-全链路集成测试-integration-testing)
   - [4. 稳定性与健壮性测试 (Robustness Testing)](#4-稳定性与健壮性测试-robustness-testing)

---

# 一、系统整体架构图

系统采用 **数据感知层 ➔ 逻辑引擎层 ➔ LLM决策引擎 ➔ 推送通知层** 的三层过滤器架构：

```text
                              ┌────────────────────────┐
                              │  AkShare & 新闻数据源  │
                              └───────────┬────────────┘
                                          │ 实时/定时抓取
                                          ▼
┌───────────────────────────────────────────────────────────────────────────────────┐
│ 1. 数据与状态管理层 (Data Layer)                                                    │
│  - 实时行情 / 涨跌停 / 连板梯队 / 龙虎榜 / 主力资金 / 政策新闻                      │
└─────────────────────────┬─────────────────────────────────────────────────────────┘
                          │ 结构化数据 (Pandas / Dict)
                          ▼
┌───────────────────────────────────────────────────────────────────────────────────┐
│ 2. 量化逻辑与策略引擎层 (Logic Layer)                                                │
│  - 情绪多维向量评分 (高度/宽度/反馈/力度/承接)                                      │
│  - 动态中军池维护 (成交额+市值+Beta相关性)                                         │
│  - 5大战法标签化归因 (低吸/打板/二波/抱团/共振)                                      │
│  - 龙虎榜席位分类 (格局派/砸盘派/对倒派)                                            │
└─────────────────────────┬─────────────────────────────────────────────────────────┘
                          │ 标签化异动 + 结构化情绪指标
                          ▼
┌───────────────────────────────────────────────────────────────────────────────────┐
│ 3. LLM 智能决策引擎 (Decision Layer)                                                │
│  - Prompt 模板工程 (复盘/简报/竞价/异动)                                           │
│  - 决策生成: 交易策略、竞价确认、操作细则 (开盘要求/竞价量能/止盈止损)              │
└─────────────────────────┬─────────────────────────────────────────────────────────┘
                          │ 润色与结构化推送文案
                          ▼
                              ┌────────────────────────┐
                              │   Bark 消息推送系统    │
                              └────────────────────────┘
```

---

# 二、开发计划（7天里程碑）

## 阶段一：项目基础设施与数据采集模块（第 1-2 天）

### 目标
搭建标准 Python 项目目录，配置统一环境，完成 AkShare 接口的二次封装（带重试、缓存与异常处理），确保数据抓取稳定可靠。

### 详细任务清单
1. **环境与配置管理**
   - 创建 `config/settings.py`：支持环境变量与配置文件，存储 LLM API Key、Bark Token、轮询间隔（默认15s）、打板/止损阈值等参数。
   - 创建 `config/prompt_templates.py`：定义盘后复盘、盘前简报、竞价观察、卖出预警的系统提示词（System Prompt）。
2. **AkShare 数据封装 (`data/fetcher.py`)**
   - **行情接口**：`stock_zh_a_spot_em`（全市场实时快照）、`stock_individual_fund_flow`（资金流向）。
   - **短线连板数据**：涨跌停池、炸板池、连板梯队统计。
   - **龙虎榜数据**：`stock_lhb_detail_em`（每日龙虎榜营业部明细）。
   - **板块与成分股**：`stock_board_industry_cons_em`（概念/行业板块成分股及权重）。
   - **异常处理机制**：封装 `@retry` 装饰器，处理 AkShare 网络超时、频率限制及字段变更问题。
3. **新闻与热度数据抓取 (`data/news_fetcher.py`)**
   - 抓取财联社、东财快讯及同花顺热搜词，提供结构化文本。
4. **Bark 推送模块 (`notifier/bark.py`)**
   - 封装 HTTP API，支持不同通知级别（普通/高优先级/报警音/分组显示）。

---

## 阶段二：量化逻辑与情绪评分引擎（第 3-4 天）

### 目标
实现核心设计中要求的 4 大进阶解决方案：情绪多维向量评分、动态中军池构建、5大战法标签化归因、龙虎榜席位画像。

### 详细任务清单
1. **情绪多维向量评分系统 (`core/emotion_index.py`)**
   - 计算 5 维指标：
     - **高度（Height）**：市场最高连板天数。
     - **宽度（Breadth）**：涨停家数 - 跌停家数。
     - **反馈（Yield）**：昨日涨停个股今日开盘平均溢价率。
     - **力度（Force）**：全市场封单资金 / 市场总成交额。
     - **承接（Support）**：炸板率（炸板家数 / (封板家数 + 炸板家数)）。
   - 输出综合情绪分值与变动趋势。
2. **动态中军与核心池 (`core/core_pool.py`)**
   - **计算规则**：选取板块内成交额 Top 3 且市值 Top 5 的个股。
   - **Beta 相关性分析**：计算个股与行业指数走势的皮尔逊相关系数（Correlation），筛选 Correlation > 0.8 且成交额 > 20 亿的标的。
   - 维护每日盘前更新的 `Active_Core_Pool`（动态核心池）。
3. **5大战法标签化归因引擎 (`core/strategies.py`)**
   - **低吸战法**：中军池成员 + 回踩 10/20 日线 + 成交量显著缩量 + 分时止跌。
   - **打板战法**：启动期首板/1进2；发酵期板块联动龙头。
   - **二波战法**：前 30 天人气龙头 + 高位腰斩回撤（30%-50%）+ 止跌反包。
   - **抱团战法**：全市场缩量/阴跌 + 标的抗跌横盘/逆势放量 + 流动性极佳。
   - **共振战法**：指数放量大阳 + 新题材大涨 + 板块内最先封板个股。
4. **龙虎榜席位画像库 (`core/seat_analyzer.py`)**
   - 构建游资营业部字典（格局派、砸盘派/拉萨天团、量化对倒派）。
   - 计算游资买卖净额与买卖比率，输出资金“神韵”诊断结论。

---

## 阶段三：LLM 大模型决策引擎开发（第 5-6 天）

### 目标
构建基于 LLM（支持 DeepSeek / OpenAI / Claude 接口）的 4 大核心决策链，实现严格格式化的策略与操作指令输出。

### 详细任务清单
1. **LLM 客户端封装 (`llm/client.py`)**
   - 支持 Async 调用、结构化 JSON/Markdown 解析、Token 统计与自动重试机制。
2. **盘后复盘模块 (`llm/post_market.py`)**
   - **输入**：涨跌停统计、连板梯队、龙虎榜席位、成交额 Top 20、情绪向量指标。
   - **LLM 任务**：归纳板块逻辑、定性情绪周期、匹配适宜战法。
   - **输出**：次日详细操作建议（针对不同场景：高位连板、弱转强、1进2分歧等，给出开盘要求、竞价量能比例、买入/卖出/止损条件）。
3. **盘前简报模块 (`llm/pre_market.py`)**
   - **输入**：08:30 抓取的政策新闻、同花顺热搜榜。
   - **LLM 任务**：提取高频关键词、评估政策级别（国家级 vs 地方级）、预测可能爆发的板块与关联标的。
4. **竞价观察模块 (`llm/call_auction.py`)**
   - **输入**：09:26 抓取的昨日连板/首板竞价撮合数据、盘前简报板块开盘异动数据、盘后复盘推荐标的竞价表现。
   - **LLM 任务**：确认竞价抢筹/弱转强信号，给出即时介入指令。
5. **卖出与风控引擎 (`core/holding_monitor.py` & `llm/sell_advisor.py`)**
   - **输入**：持仓标的实时分时价格、分时均线、关键均线（5日线）、全市场连板断板情况。
   - **触发规则**：情绪到顶（连板数>8）、跌破分时均线30分钟未收回、跌破5日线、龙头断板必卖。

---

## 阶段四：定时调度与盘中 15 秒轮询监控系统（第 7 天）

### 目标
通过 `APScheduler` 与异步轮询建立交易日全流程自动化流水线。

### 详细任务清单
1. **交易日时间线调度器 (`scheduler/daily_runner.py`)**
   - **08:30**：触发 [盘前简报] 任务 ➔ 推送 Bark。
   - **09:26**：触发 [竞价观察] 任务 ➔ 获取竞价数据 ➔ LLM 生成竞价指令 ➔ 推送 Bark。
   - **15:30**：触发 [盘后复盘] 任务 ➔ 计算情绪指标与中军池 ➔ LLM 深度复盘与生成次日计划 ➔ 推送 Bark。
2. **盘中 15 秒实时轮询系统 (`scheduler/market_monitor.py`)**
   - 交易时间（09:30-11:30, 13:00-15:00）每 15 秒抓取全市场快照。
   - **点火预警**：单笔/短期成交量达过去 5 日均值 3 倍且涨幅 > 3%。
   - **主力扫货**：资金流向监测。
   - **炸板提醒**：持仓/连板股封单骤减或炸板。
   - **卖出风控**：实时触发破位止损/断板卖出提醒。

---

# 三、项目目录结构说明

```text
dragon_pulse_plus/
├── SYSTEM_PLAN.md           # 本开发与测试计划文档
├── 核心设计.txt              # 原始需求与设计说明
├── config/                  # 配置文件与Prompt模板
│   ├── settings.py          # LLM API Key, Bark Token, 阈值配置
│   └── prompt_templates.py  # 盘后、盘前、竞价、异动 Prompt 模板
├── core/                    # 核心逻辑与策略引擎
│   ├── emotion_index.py     # 情绪多维向量评分 (高度/宽度/反馈/力度/承接)
│   ├── core_pool.py         # 动态中军与核心池维护 (Beta相关性+动态权重)
│   ├── strategies.py        # 5大战法规则引擎与标签化归因
│   ├── seat_analyzer.py     # 龙虎榜席位画像分析
│   └── holding_monitor.py   # 持仓卖出风控监测
├── data/                    # 数据采集与缓存 (AkShare)
│   ├── fetcher.py           # 行情、涨跌停、连板梯队、资金流向等接口封装
│   └── news_fetcher.py      # 新闻、同花顺热搜抓取
├── llm/                     # LLM 智能决策引擎
│   ├── client.py            # LLM API 统一调用封装 (DeepSeek / OpenAI / Claude)
│   ├── post_market.py       # 盘后深度复盘逻辑
│   ├── pre_market.py        # 盘前简报逻辑
│   ├── call_auction.py      # 竞价观察与介入指令
│   └── sell_advisor.py      # 卖出建议润色
├── notifier/                # 消息推送
│   └── bark.py              # Bark 消息推送接口
├── scheduler/               # 任务调度与盘中轮询
│   ├── daily_runner.py      # 定时任务流水线 (08:30 / 09:26 / 15:30)
│   └── market_monitor.py    # 盘中 15s 轮询监控
├── main.py                  # 系统启动入口
└── requirements.txt         # 项目依赖依赖包列表
```

---

# 四、测试计划

## 1. 单元测试 (Unit Testing)

| 测试模块 | 测试内容 | 预期结果 |
|---|---|---|
| **AkShare 接口** | 测试 `data/fetcher.py` 各函数在休市/开市期间的返回值 | 正确返回 DataFrame / Dict，字段完整，网路异常时自动重试 |
| **情绪多维向量** | 输入预设的涨跌停与连板数据，测试 `emotion_index.py` | 准确输出 Height, Breadth, Yield, Force, Support 及 0-100 综合分值 |
| **动态中军池** | 输入历史行情数据，计算 Pearson Beta 系数 | 准确筛选出市值/成交额符合要求且 Beta > 0.8 的中军个股 |
| **战法标签归因** | 模拟股价异动（如回撤20日线放量止跌、断板回撤30%等） | 正确打上 `[中军回踩]`、`[二波预警]`、`[板块共振]` 等标签 |
| **龙虎榜席位解析** | 输入含“六一中路”、“拉萨天团”的龙虎榜 JSON | 识别派系分类，输出多空格局分析诊断 |
| **Bark 推送** | 发送测试消息到 Bark API | 手机端成功收到通知，样式与声音正确 |

---

## 2. 离线数据模拟测试 (Historic Replay / LLM Evaluation)

### 目的
利用历史交易日的数据（如特定爆发行情日、极度冰点退潮日），验证 LLM Prompt 生成的复盘策略与交易指令的合理性。

### 测试用例设计
1. **典型情绪高潮日测试**
   - **数据集**：提供某一历史高潮日（如全市场40+家涨停、最高板8连板）的数据。
   - **验证点**：LLM 能否准确识别出“高潮末端”，提示次日防范分歧砸板，并转向防守策略或抱团/低吸战法。
2. **冰点转启动日测试**
   - **数据集**：提供指数量能见底后放量大阳 + 新主线首板共振的数据。
   - **验证点**：LLM 能否定性为“启动期/共振”，并给出次日积极打板/1进2抢首板的操作建议。
3. **输出格式严密性测试**
   - **验证点**：连续运行 20 次 LLM 策略生成，验证输出的 JSON/Markdown 结构是否 100% 包含：开盘要求（幅度）、竞价量能比例、买入条件、止损止盈位。

---

## 3. 全链路集成测试 (Integration Testing)

### 目的
在非交易时间模拟“交易日时间线”（通过 Mock 盘口数据与模拟时钟），测试系统自动化流水线的衔接。

### 集成测试流水线
1. **模拟 08:30** ➔ 启动盘前简报，确认新闻提取与预测板块正常输出。
2. **模拟 09:26** ➔ 注入模拟竞价数据，确认竞价观察模块匹配盘前预测并下达指令。
3. **模拟 09:30-10:00 盘中** ➔ 注入 15 秒轮询数据，触发模拟异动（如某股突破3%且放量3倍），确认异动归因标签生成并推送。
4. **模拟卖出触发** ➔ 注入持仓股跌破 5 日线或炸板数据，确认实时触发 [卖出提醒] 推送。
5. **模拟 15:30** ➔ 触发盘后自动复盘，校验动态中军池更新与策略导出。

---

## 4. 稳定性与健壮性测试 (Robustness Testing)

1. **网络超时与 API 挂起测试**
   - 模拟 AkShare 或 LLM API 响应延迟 10 秒或超时，验证系统是否有 Timeout 处理，不会导致盘中轮询卡死。
2. **数据字段缺失/空值测试**
   - 模拟部分股票无龙虎榜、竞价数据为空、涨停家数为0等极端边界情况，确保系统不报 `KeyError` 或 `IndexError`。
3. **15 秒轮询性能测试**
   - 监控 15 秒轮询任务在处理全市场 5000+ 只股票快照时的 CPU/内存占用与耗时（需控制在 3-5 秒内完成计算）。
