# dragon_pulse_plus 监控规则总文档

> 本文档汇总系统现有的全部监控/风控/交易规则，所有阈值均核对自 `config/settings.py` 与相关代码。修改阈值以 `.env` 覆盖为准；修改判定逻辑以代码为准。文档按监控链路组织。

---

## 目录
1. [总体运行框架](#一总体运行框架)
2. [盘中主循环与时间窗口](#二盘中主循环与时间窗口)
3. [竞价与盘前规则](#三竞价与盘前规则)
4. [四类抢筹信号](#四四类抢筹信号)
5. [买入评估流程](#五买入评估流程)
6. [买入闸门（硬性风控）](#六买入闸门硬性风控)
7. [卖出规则（六条风控 + 专项卖出）](#七卖出规则)
8. [其他盘中监控](#八其他盘中监控)
9. [监管异动红线](#九监管异动红线)
10. [板块周期 / 概念主线 / 中军核心池](#十板块周期--概念主线--中军核心池)
11. [市场情绪 / 风格 / 周期](#十一市场情绪--风格--周期)
12. [看门狗与自愈](#十二看门狗与自愈)
13. [数据源超时与熔断](#十三数据源超时与熔断)
14. [阈值速查表](#十四阈值速查表)

---

## 一、总体运行框架

监控由 `main.py` 常驻进程承载，几套定时任务并行：

| 时间 | 任务 | 功能 |
|---|---|---|
| 04:00 | 日志清理 | 系统/LLM/错误日志保留 15 天，推送保留 30 天 |
| 04:05 | 龙头过期 | 历史龙头 >30 天自动失效 |
| 08:30 | 盘前简报 | 新闻+热搜 → LLM 预测板块 → Bark 推送 |
| 09:15-09:25 | 竞价观察窗口 | 每 30 秒采集竞价快照、量能趋势 |
| 09:26 | 竞价观察 | 竞价快照+推荐标的 → LLM 买卖指令 → Bark 推送 |
| 09:30-11:30 / 13:00-15:00 | 盘中实时监控 | 15 秒轮询：抢筹信号扫描 + 自动买卖 + 各类预警 |
| 14:30-15:00 | 尾盘博弈窗口 | 独立策略，低吸强势股博次日高开 |
| 18:01 | 盘后深度复盘 | 情绪→风格→LLM 复盘→指数/涨停池/板块落库→盈亏报告 |
| 20:00 | 假日消息汇总 | 假期最后一天推送 |

---

## 二、盘中主循环与时间窗口

主循环 `run_polling_loop`（`scheduler/monitor_core.py`）：

- 非交易日：每 **5 分钟**检查一次（`time.sleep(300)`），不浪费 API。
- 竞价窗口 `is_auction_time()`（09:15-09:25）：每 **30 秒**采集一次竞价快照。
- 交易时段 `is_trading_time()`（09:30-11:30 / 13:00-15:00）：每 **15 秒**（`MONITOR_INTERVAL_SECONDS=15`）跑一轮 `_check_realtime_market`。
- 尾盘博弈窗口 `is_tail_end_time()`（14:30-15:00）：`_scan_tail_game` 内部按此窗口门控（指南：避开 14:00-14:30 跳水段，买在走势定型后）。
- 每轮执行顺序（`_check_realtime_market`）：
  1. 换日重置当日去重状态（`_reset_daily_state`，盘中重启后从数据库重建当日已买集合）。
  2. 08:30 盘前简报缺失时自愈补发一次。
  3. 刷新涨停/炸板池缓存（60 秒间隔，失败退避 300 秒）。
  4. 拉全市场快照。
  5. 计算最高连板/炸板率、判定市场风格、检查大盘熔断。
  6. **持仓卖出/止损监控前置**（先于买入扫描，绝对止损等硬护栏不因买入 LLM 同步调用被拖后）。
  7. 扫描四类抢筹信号 + 买入 + 推送。
  8. 尾盘博弈（14:30-15:00）。
  9. 龙头二波（全天盘中）。
  10. 炸板预警、情绪到顶预警、一致性预警。

> **周期时间预算**（`MONITOR_CYCLE_BUDGET_SECONDS=90`）：慢源（如东财限流后腾讯降级 ~80s）把单轮拖长时，超过预算**跳过非关键步骤**（资金流检测、尾盘博弈、龙头二波、炸板/情绪到顶/一致性预警）提前收尾，买入/卖出 LLM 超预算时降级或推迟——防止单轮拖过看门狗 120s 阈值触发自动重启。**须小于 `WATCHDOG_STALL_SECONDS`。**

---

## 三、竞价与盘前规则

### 3.1 盘前简报（08:30，`scheduler/pre_market.py`）

- 数据源：财联社快讯 `get_cls_news(limit=15)`（15 条，正文截取前 **100** 字符）+ 同花顺热搜 `get_hot_search_words(limit=15)`。
- 前置：同步「立案调查/违规处罚」黑名单。
- LLM 输出要求：预测最可能爆发的 **1-3 个**热点板块并给理由；区分政策级别（国家级/地方级/企业吹风会）。
- 落库：内存缓存（供 09:26 快速读取）+ 数据库 `PreMarketReportManager`（重启不丢）。
- 推送：`☀️ 盘前简报与爆发题材预测`，group=盘前简报。

### 3.2 竞价量价监控（09:15-09:25，`scheduler/monitor_auction.py`）

每 30 秒采集全市场快照，字段：全市场成交额、高开 ≥ **+5.0%** 只数、低开 ≤ **-5.0%** 只数、平均涨幅。**09:24-09:25** 推送一次竞价预判（去重）。

- **竞价量能趋势**：末拍成交额 > 首拍 × **1.5** → 放量；< 首拍 × **0.7** → 缩量；否则平稳。
- **大盘预判**（强/弱/中性 × 昨日情绪周期 → 结论）：
  - 强开盘：平均涨幅 ≥ 0.5 且 高开只数 ≥ 20 且 放量 → 按周期给出"高开高走/警惕高开低走/诱多"等结论；
  - 弱开盘：平均涨幅 ≤ -0.3 → 按周期给出"低开高走可期/防守/减仓观望"等结论；
  - 中性：结合昨日上证涨跌与周期 → 震荡偏强/偏弱/等待 09:30 确认。
- **板块延续预判**：热门板块前 3 个，成分股在竞价快照匹配 ≥ 2 只才评估；高开率（>**+2.0%** 算高开）> **50%** → 延续（资金继续攻击）；< **20%** → 一日游风险；其余 → 分歧待开盘确认。
- **竞价龙头**：全市场涨幅前 5 只。
- 预判结果缓存供 09:26 LLM 竞价指令读取；推送 `📊 [竞价预判]`，group=竞价指令。

### 3.3 竞价观察与指令（09:26，`scheduler/auction.py`）

**候选四路**：
1. 昨日 LLM 复盘推荐（`status=PENDING`）；
2. 昨日涨停池连板/首板前 **15** 只（lbc≥2 → "N连板"，否则"首板"）；
3. 盘前简报预测板块（当日报告前 800 字符，优先 DB 再内存缓存）；
4. 实时竞价快照。

**LLM 逐票下判**（`llm/call_auction.py`）：
- 上下文：昨日涨停溢价（取不到默认 1.5%）、规则引擎竞价预判、推荐标的竞价情况。
- 双路解析：结构化 JSON 块 `{"verdicts":[{code,verdict,premise,reason}]}` 优先（code 必须 6 位数字）；回退关键词分类（"放弃"关键词 / "买入"关键词，默认观察）。
- 判定标准（prompt 内）：昨日分歧大/烂板今日竞价**高开 > +2%** 且**竞价金额占昨日全天成交额 > 8%~10%** → 超预期抢筹；高位连板开在 **-5%** 或跌停 → 全市场避险提示。
- 前提约束：判断=买入 必须同时 前提=满足 才会被执行；否则记"竞价矛盾"不执行。
- 落库 `RecommendationManager.update_auction_verdicts`（限定 上一交易日 + PENDING，防写错旧记录）。
- 推送：`🎯 09:26 竞价超预期指令`，group=竞价指令（Bark 层去掉 JSON 块，只推可读内容）。

**verdict 的盘中消费**（`monitor_core.py`）：判定=买入 且 前提=满足 的推荐标的**无需命中技术信号**即进候选池评估买入（仍过闸门/封板/持仓限制）；判定=买入但前提不满足→不执行并告警；评估过的一次性标记，但封板/不在快照的不标记（破板/快照齐全后可重新进池）。

**开盘要求校验**（`_check_open_requirement`）：区间 `+3%~+6%`→含端点；`>3%`/`≥3%`→≥3.0；含"高开"→≥2.0；含"平开/低开"→≤1.5；无要求→放行。

---

## 四、四类抢筹信号

信号由 `core/signal_flags.py::compute_signal_flags` 纯函数计算，**实盘与回测共用同一套阈值**。按板块区分涨停线：主板（60/00 开头）10cm，双创（30/688）20cm。

| 信号 | 触发条件（全部满足） | 标签优先级 |
|---|---|---|
| **逼近封板** | 涨幅 ∈ [涨停线×0.84, 涨停线] 且 量比 > 5.0 | 1 |
| **低开猛拉** | 开盘 < 昨收×0.98 且 量比 > 3.0 且 拉升强度 > 0.8 且 拉升幅度 ≥ `RALLY_MIN_PCT=2.0%`（现价相对开盘，%昨收）且 涨幅 > 0；拉升强度 = (现价−开盘)/max(最高−最低, 昨收×`RALLY_DENOM_MIN_RATIO=1%`)（分母保护防早盘微小区间刷强度） | 2 |
| **点火异动** | 量比 ≥ 3.0 且 涨幅 ≥ 3.0% 且 涨幅 < 涨停线（已涨停不算） | 3 |
| **振幅放量** | 振幅 > 7.0% 且 量比 > 3.0 且 涨幅 > 3.0% | 4 |

说明：
- 信号优先级顺序：逼近封板 → 低开猛拉 → 点火异动 → 振幅放量（`primary_signal_label` 取首个命中）。
- 涨停线：主板 `PRICE_BURST_MAX=9.5`，双创 `PRICE_BURST_MAX_20CM=19.5`；逼近封板区间下限 = 涨停线 × `MONITOR_NEAR_LIMIT_RATIO=0.84`。
- 一只股票可同时命中多个信号（`signal_labels` 全列）。

---

## 五、买入评估流程

`_scan_signals`（`scheduler/monitor_core.py`）对命中信号（`_signal_hit`）的候选逐一评估：

1. **排序**：按市场风格（`priority_strategy`）调整排序：打板接力→逼近封板→涨幅→量比；中军回踩→低开猛拉→量比→涨幅；避险抱团→振幅放量→量比；其他→量比→涨幅。
2. **合并竞价"买入"指令**（`_merge_auction_buy_candidates`）：09:26 竞价 LLM 判定"买入"且前提"满足"的推荐标的，无信号也进候选池（仅评估一次）。
3. 每轮最多评估 `hit_df.head(5)` 个候选。
4. **当日去重**：已推送过的 code 跳过（推荐标的 09:26 一次性评估后同样锁）。
5. **推荐标的买点验证**（`_check_rec_buy_condition`）：竞价未判"买入"的，需满足 open_requirement + 竞价量能正则；任何情况下相对开盘回落 > `REC_FADE_MAX=2.0%` 不买（活的安全网）。
6. **高质量信号判定**（跟随风格）：打板接力→逼近封板；中军回踩→低开猛拉；避险抱团→振幅放量；共振/观望→任一信号（点火异动需 涨幅≥5% 且 量比≥5 且 成交≥2亿 才高质）。
7. **情绪周期约束**（`cycle_stance`）：冰点/退潮 `allow_auto_buy=False` 禁止自动买入；启动期 `only_recommended=True` 只买推荐标的；发酵/高潮放开。
8. **竞价 verdict 门控**：09:26 判定"放弃"的推荐标的不自动买入（仍推送异动提醒）。
9. **封板买不进**：已涨停（`_is_limit_up`，主板≥9.8/双创≥19.8）→ 推送"封板待重评"（独立去重，**不锁**当日去重），破板后重新进池。
10. **市场风格否决**：风格=观望时不自动买入（推荐标的除外）。
11. **板块因子否决**：所在板块阶段为 退潮/冰点 否决；高潮但非主线否决（只做主线高潮龙头）。
12. **概念因子否决**（`_get_concept_blocks_buy`）：**仅当全部概念均明确负向**（退潮/冰点/高潮非主线）才否决；存在任一可买概念（发酵/启动/主线高潮）即放行；无概念数据/无周期记录（未知）放行（防误杀数据未覆盖题材）。
13. **买入闸门**（见下节）前置到 LLM 调用之前（满仓/熔断/形态不佳不白烧 LLM）。
14. **LLM 买入确认**（B 方案）：每轮最多 `LLM_BUY_CONFIRM_PER_CYCLE=1` 次同步 LLM（控制 15s 主循环阻塞）；预算用尽→推送"预算待重评"不锁，下轮重新评估；LLM 判观望/放弃→不买；LLM 失败→**降级回规则买入**。
15. **买前复核**（`_recheck_buy_after_llm`）：LLM 等待期间价格变化，买前用最新快照复核——已封板→不买(下轮重试)；已跌停→不买(当日结束)；相对开盘回落>2%→不追；快照不可用→下轮重试。
16. **二次过闸门** → 执行买入，记录滑点后成交价。

**成交滑点模型**（`_compute_buy_cost`）：普通信号买入成本 = 现价 × (1 + `AI_BUY_SLIPPAGE_PCT=0.3%`)；**逼近封板/高位放量信号默认按涨停价撮合**（`AI_BUY_NEAR_LIMIT_FILL_LIMIT=True`，成本 = 昨收 × (1+涨停线)，打板资金实际多撮合在涨停附近，原 0.5% 滑点对回测过于乐观）；关闭该开关时退回 0.3%+`AI_BUY_SLIPPAGE_HOT_PCT=0.2%` 滑点模型。尾盘博弈/二波买入走普通滑点。

**尾盘博弈**（14:30-15:00，`_scan_tail_game`）：候选 = 尾盘博弈信号（涨幅 2%~5% + 量比≥3 + 收阳 + 上影线占比≤0.3 + 收盘≥均价 VWAP）→ 站上 MA5/MA10（`TAIL_GAME_REQUIRE_MA=True`，MA 缺失**明确告警并放行**）→ 独立闸门 → 独立 LLM 确认（每轮 `TAIL_LLM_PER_CYCLE=1`）→ 买前复核 → AI_TAIL 持仓。次日 09:30-10:30 早盘兑现（见卖出节）。

**龙头二波**（全天 9:30-15:00，`_scan_second_wave`）：近 `SECOND_WAVE_LOOKBACK_DAYS=30` 天历史龙头（`peak_price`）+ 现价回撤 **30%~50%** + 当日涨幅 > `SECOND_WAVE_CHANGE_MIN=3.0%`（止跌反包）→ 站上 MA5/MA10 → 独立闸门 → 独立 LLM 确认（每轮 `SW_LLM_PER_CYCLE=1`）→ AI_SW 持仓。回撤越浅+涨幅高优先。卖出：突破前高兑现 / `SW_HOLD_DAYS=5` 天未创新高离场。

---

## 六、买入闸门（硬性风控）

`_buy_gates_open`（白天 AI 买入）——任一不满足即拦截：

| 闸门 | 阈值 | 说明 |
|---|---|---|
| 持仓上限 | `MAX_AI_POSITIONS=5` | AI 自动持仓达上限不再买 |
| 当日次数 | `MAX_DAILY_BUYS=3` | 当日 AI 自动买入次数上限 |
| 当日亏损熔断 | `DAILY_LOSS_CIRCUIT_BREAKER=-5%` | AI 持仓当日平均盈亏（相对昨收）≤ 此值停止买入 |
| 分时形态 | `PATTERN_CHECK_MIN_AMOUNT=5亿` | 成交额≥5亿才检测分时形态；冲高回落/放量滞涨/天地板/尾盘砸盘 → 不买（TTL 缓存 300 秒） |
| 板块集中度 | `MAX_AI_SECTOR_POSITIONS=2` | 同板块 AI 持仓已达 2 只则跳过 |
| 监管异动 | `REGULATORY_GATE_ENABLED=true` | 今日涨停将触发交易所异动/停牌核查（一级/二级）→ 拦截（`daily_kline`+`market_index` 本地算） |
| 已持仓 | — | 已持有该 code 不重复买 |

**尾盘博弈独立闸门** `_tail_gates_open`：`TAIL_MAX_POSITIONS=2`（独立持仓）、`TAIL_MAX_DAILY_BUYS=2`（独立当日次数，不吃白天预算）、大盘熔断、已持仓、**共享**当日亏损熔断（AI_AUTO+AI_TAIL 合计）。

**龙头二波独立闸门** `_second_wave_gates_open`：`SW_MAX_POSITIONS=2`、`SW_MAX_DAILY_BUYS=2`、大盘熔断、已持仓、**共享**当日亏损熔断（AI_AUTO+AI_TAIL+AI_SW 合计）。

**大盘级熔断** `_check_index_breaker`：全市场均涨幅 ≤ `INDEX_DROP_CIRCUIT_BREAKER=-2%` → 停止**所有**自动买入（含尾盘博弈/二波），每日仅推送一次。

---

## 七、卖出规则

### 7.1 六条风控规则（`core/holding_monitor.py::check_sell_signals`）

按优先级排列，每条独立判定、可同时触发多条：

| 优先级 | 规则 | 级别 | 触发条件 | 动作 |
|---|---|---|---|---|
| 0 | **绝对止损** | CRITICAL | 亏损 ≥ `ABSOLUTE_STOP_LOSS_PCT=-7%` | 无条件止损，**立即返回不再查后续规则** |
| 1 | **断板必卖** | CRITICAL | 今日曾封板 + 当前未封 + 跌破 VWAP 分时均线 | 自动平仓（排除正常二封回封） |
| 2 | **破位止损** | HIGH | 打板/接力/AI自动 → 跌破 VWAP；低吸/中军 → 跌破 MA5 | 自动平仓 |
| 3 | **情绪到顶预警** | WARNING | 全市场连板 ≥ 8 且 炸板率 > 35% | 全局预警（非单股） |
| 4 | **时间止损** | WARNING | 持仓 ≥ 3 **交易日** 且 未盈利（盈亏 ≤ 0） | 预警 |
| 5 | **逢高止盈** | HIGH / WARNING | 盈利 ≥ 20% **且 自当日高点回落 ≥ 5%**（强止盈）/ ≥ 15%（止盈提醒） | 预警 |

> ✅ **已修复（2026-08-07）**：① 强止盈补"自当日高点回落 ≥ `TAKE_PROFIT_HIGH_PULLBACK_PCT`（默认5%）"复合判定——未回落（仍在主升浪）只给 WARNING 提醒，不强行平仓，防打断妖股/连板；② 时间止损改**交易日**计算（`count_trading_days`，日历覆盖范围外按工作日回退），消除自然日跨周末误触发（周五买周一不再误判为持有3天）。已知局限：回测尾盘兑现用**全天最高价**（日线模型无分时数据），精确的 09:30-10:30 窗口高点需分钟数据，见 7.3 备注。

### 7.2 卖出执行逻辑（`_monitor_holdings`）

- **T+1**：当日买入的持仓当日不可卖（跳过卖出信号检查，价格更新照常）。
- **CRITICAL/HIGH 信号 → 自动平仓**：
  - **绝对止损为硬护栏**，规则直接卖（不经过 LLM）。
  - 其余（断板/破位/止盈/时间止损）走 **LLM 复核（B 方案）**：LLM 判"出货"→卖；判"持有"→不卖，并进入 `LLM_SELL_HOLD_COOLDOWN_SECONDS=1800s` 冷却（冷却期内不重复咨询，避免持续信号每 15s 阻塞 LLM）；冷却期内现价较 LLM 决策价急跌 ≥ `LLM_SELL_COOLDOWN_BREAK_PCT=3%` → **打破冷却**立即重新评估（防闪崩/退潮硬扛，绝对止损仍兜底）；LLM 失败→降级回规则卖。
  - **跌停锁定**：触发卖出但当前跌停 → 加入 `_pending_sell_codes` 等破板；破跌停板后自动执行卖出（"破板卖出"通知）。
- **AI 卖出滑点**：`AI_SELL_SLIPPAGE_PCT=0.3%`（成交价低于现价）。
- 卖出信号当日按 (code, holding_type) 键去重，仅推送一次。

### 7.3 专项卖出（独立策略持仓）

**尾盘博弈 AI_TAIL 次日早盘兑现**（次日 09:30 起，绝不过夜第 2 天）：
- 未高开（开盘 < 成本×（1+`TAIL_GAME_OPEN_GAP_PCT=2%`））→ 按开盘价兑现/止损；
- **高开 → 动态移动止盈**（实盘）：从当日最高（09:30 起）回落 ≥ `TAIL_GAME_TRAIL_PULLBACK_PCT=3%` → 实时价卖出；未回落持有到 **10:30 强平**（慢周期也不漏）。不再"假设在开盘与最高点中点成交"（原模型偏乐观）。
- **回测口径**（消除 look-ahead）：日线无分时数据，09:30-10:30 窗口最高价按 开盘×（1+`TAIL_GAME_MORNING_HIGH_CAP_PCT=5%`）封顶后按 `TAIL_GAME_TAKE_RATIO=0.5` 冲高一半兑现——全天最高出现在 10:30 之后也不虚高回测收益。

**龙头二波 AI_SW**：
- **突破前高兑现**：当日最高价 ≥ 第一波峰值（记录在持仓策略 `PEAK{price}` 中）→ 按最高价止盈卖出；
- **N 天未创新高离场**：持仓 ≥ `SW_HOLD_DAYS=5` 天且未突破前高 → 按现价清仓离场；
- 绝对止损/破 MA5/强止盈仍走常规 7.1 规则。

---

## 八、其他盘中监控

| 监控 | 触发条件 | 通知 |
|---|---|---|
| **大盘熔断** | 全市场均涨幅 ≤ -2% | 🛑 [大盘熔断] 停止所有自动买入 |
| **情绪到顶预警** | 最高连板≥8 且 情绪分≥75 且 炸板率>35% | 🚨 [情绪到顶预警] 高潮末端 |
| **一致性预警** | 情绪分≥70 且 涨停≥80 家 | ⚠️ [一致性预警] 明日分歧风险 |
| **风格切换** | 市场风格变化（抱团/共振/打板/低吸/高潮/观望） | 风格切换通知 |
| **板块联动**（`_check_sector_linkage`） | 同板块涨停家数 ≥ `SECTOR_LINKAGE_MIN_COUNT=3` 且 较上轮增量 ≥ `SECTOR_LINKAGE_ACCEL_DELTA=2` | 🔗 [板块联动]（开关 `SECTOR_LINKAGE_PUSH_ENABLED`） |
| **封单衰减**（`_check_seal_decay`） | 持仓股封单较上轮缩减 ≥ 70% | ⚠️ [封单衰减] 炸板风险上升 |
| **大单抱团**（`_check_fund_inflow_alert`） | 点火异动股主力净流入 > max(`FUND_INFLOW_MIN=2000万`, 流通市值 × `FUND_INFLOW_CAP_RATIO=0.0005`) | 记录日志（最多查 3 只） |
| **炸板预警**（`_check_zhaban_alert`） | 涨停池标的当前涨幅 < `ZHABAN_ALERT_CHANGE=7%` 且 量比 > `ZHABAN_ALERT_VOL_RATIO=2.0` | 💥 [炸板提醒] 高位分歧 |

真炸板口径（全系统统一）：在炸板池但不在涨停池的股票数；炸板率 = 真炸板/(涨停+真炸板)×100。

---

## 九、监管异动红线

`core/regulatory_yidong.py`，总开关 `REGULATORY_MONITOR_ENABLED=True`。

**3 日偏离度红线**（个股近3日涨幅 − 指数3日涨幅）按板类型：
| 板类型 | 红线 dev_3d_limit | 一级异动警示线（红线−6） |
|---|---|---|
| 主板 | `MAIN_BOARD_3D_DEV_LIMIT=20%` | 3日偏离 ≥ **14%** |
| 创业板 / 科创板 | `GEM_3D_DEV_LIMIT` / `STAR_3D_DEV_LIMIT=30%` | 3日偏离 ≥ **24%** |
| 北交所 | 硬编码 **45%** | 3日偏离 ≥ **39%** |

- **一级异动警示（WARNING_YIDONG）**：`dev_3d >= dev_3d_limit - 6.0` → 提示"今日若再涨停必触发官方《交易异常波动公告》"。
- **二级严重异动（CRITICAL_SERIOUS）**：`dev_10d >= REGULATORY_10D_LIMIT - 15.0`（即 10 日偏离 ≥ **85%**）**或** 10 日内异动次数 ≥ `MAX_YIDONG_COUNT_10D - 1`（即 ≥ **3 次**）→ 提示"再涨停极易触发停牌核查/重点监控，建议严格避开/逢高落袋"。
- 10 日严重异动红线：`REGULATORY_10D_LIMIT=100%`。
- **盘中买入闸门**（`REGULATORY_GATE_ENABLED=True`）：命中一级/二级 → 禁止自动买入（"今日涨停将触发异动/停牌核查"）。个股 3/10 日累计涨幅（含今日假设涨停）取 `daily_kline` 缓存，指数偏离度取 `market_index` 表（60→上证、00→深证、30→创业板），**本地零网络**；数据不足不拦截（未知即放行）。

---

## 十、板块周期 / 概念主线 / 中军核心池

### 10.1 板块阶段判定（`core/sector_cycle.py::derive_phase`）

按顺序短路判定（cur_zt=当日板块涨停家数，max_lbc=板块内最高连板，history=近5日涨停序列，peak=max(history)）：

1. `cur_zt ≤ 0` → **冰点**
2. `cur_zt ≥ 5 且 (max_lbc ≥ 4 或 创新高)` → **高潮**
3. `peak ≥ 4 且 cur_zt ≤ max(1, peak×0.5)` → **退潮**（曾高位现滑落）
4. `cur_zt ≥ 3 或 (cur_zt ≥ 2 且 max_lbc ≥ 3)` → **发酵**（单只妖股不算）
5. 上日为 发酵/高潮 且 `cur_zt ≤ 2` → **退潮**
6. 其余（1-2 家涨停）→ **启动**

### 10.2 主线分与主线识别

- `mainline_score = 0.3×涨停归一(cur_zt/8) + 0.3×持续归一(appear_days/5) + 0.2×加速归一(accel/3) + 0.2×高度归一(max_lbc/5)`，各维度钳位 0~1。
- **板块** `is_mainline`：`mainline_score ≥ 0.5`（`database/sector_cycle.py` 硬编码 0.5）。
- **概念** `is_mainline`：`mainline_score ≥ CONCEPT_MAINLINE_SCORE_THRESHOLD=0.5`。
- 概念先过滤事件型/属性型非题材标签（含"业绩/重组/ST/参股/重仓"等黑名单，兜底视为题材 THEME 保留），仅题材型概念参与周期计算。

### 10.3 盘中板块/概念门控（消费方 `monitor_core.py`）

- 板块：阶段 ∈ {退潮, 冰点} 否决；高潮但非主线否决；发酵/启动/主线高潮放行。
- 概念（开关 `CONCEPT_GATE_ENABLED=True`）：**仅全部概念均明确负向**才否决；任一可买概念 / 无概念数据 / 概念无周期记录（未知）均放行（"未知即放行"，防误杀数据未覆盖题材）。

### 10.4 中军核心池（`core/core_pool.py`）

- 成交额门槛：`CORE_POOL_MIN_AMOUNT=12` 亿；过滤后为空则回退按成交额降序取 `CORE_POOL_TOP_AMOUNT=3` 只。
- TopN 选取：成交额 Top `CORE_POOL_TOP_AMOUNT=3` ∪ 总市值 Top `CORE_POOL_TOP_MARKET_CAP=5`；市值数据全缺失时用成交额 TopN 替代。
- Beta 相关性：`CORE_POOL_MIN_BETA=0.8`，与板块指数（缺省用上证指数近30日）Pearson 相关 < 0.8 剔除；基准序列不可得时**不启用 Beta 过滤**（放行）。

---

## 十一、市场情绪 / 风格 / 周期

### 11.1 情绪指数（`core/emotion_index.py`）

**盘后 6 维**：反馈(昨日涨停今日开盘平均溢价)、破规胆量、宽度(涨停−跌停)、高度(最高连板)、承接(炸板率)、力度(封单/成交额)。

| 维度 | 权重 | 打分 |
|---|---|---|
| 反馈 | 25% | 负溢价：`max(min((p+3)*100/7, 100), 0)`；正溢价：`50 + min(p,4)*10 + max(0, p-4)*1.25`（0%→50 分中性锚点） |
| 破规胆量 | 20% | 晋级率%，钳位 0~100 |
| 宽度 | 20% | 负：`40+b`（b<0，每少1家-1分）；正：`40 + b*(60/90)`（90 家封顶 100） |
| 高度 | 15% | 查表 `{0:0,1:15,2:30,3:50,4:65,5:78,6:88,7:95}`，≥8 板→100 |
| 承接 | 10% | `max(100 - 炸板率×2.5, 0)` |
| 力度 | 10% | `min(封单占比×500, 100)` |

**盘中 4 维**：只用 溢价/宽度/高度/承接，权重 `0.36/0.29/0.21/0.14`（盘后 6 维去掉力度+破规胆量后按比例归一）。`0% 溢价=50 分` 中性锚点。

### 11.2 市场风格（`core/strategies.py::MarketStyle`）

- **容量因子 K** = 今日预估成交额 / 20 日均成交额，钳位 `[0.7, 1.5]`。K>1.2 增量溢出可激进；0.8<K<1.2 常规；K<0.8 流动性枯竭只能抱团。
- 动态阈值均随 K 缩放（跌停恐慌线 = 10×√K，打板涨停区间 = 30K~50K，低吸 = 40K，炸板容忍 = 25/√K×递增等）。
- **五风格评分**（trap/ramp 平滑函数）：抱团 = max(跌停恐慌成分, 0.8×溢价崩塌成分)（阈值 0.5）；高潮 = 0.5×涨停区间 + 0.3×高度 + 0.2×情绪分（阈值 0.5）；共振/打板/低吸三个攻击风格（阈值 0.55）。
- **判定优先级**：抱团 ≥ 0.5 → 抱团/避险抱团；高潮 ≥ 0.5 → 高潮/观望；攻击风格最高分 ≥ 0.55 → 共振/打板/低吸；全不满足 → 观望。
- **StyleAnalyzer 战法标签**：二波预警 / 中军回踩（中军池且 -2~3%）/ 板块共振（指数≥1% 且 板块活跃≥3 且 个股≥5%）/ 避险抱团（指数<-0.5% 且 成交<7000亿 且 上涨）/ 打板接力（涨幅≥涨停线，主板9.5/双创19.5/北交所30）/ 观望兜底。

### 11.3 情绪周期状态机（`core/cycle_machine.py`）

阶段：`冰点 → 启动 → 发酵 → 高潮 → 退潮`（严格顺序推进，不可跳级）。

- **紧急降级**：跌停数 ≥ 20 或 溢价 ≤ -4% → 直接判冰点（无视昨日阶段）。
- 转换规则（摘关键）：
  - 冰点→启动：溢价≥0 且 跌停<10 且 情绪≥30；
  - 启动→发酵：高度≥4 且 涨停≥30 且 溢价≥1 且 炸板率<30；启动→冰点：溢价≤-2 或 跌停≥15；
  - 发酵→高潮：情绪≥65 且（涨停≥60 或 高度≥7）；发酵→启动：溢价≤-1 或 情绪<35；
  - 高潮→退潮：（炸板率≥35 且 溢价<1）或 溢价≤-1.5；
  - 退潮→冰点：溢价≤-2 且 跌停≥10 且 情绪<30；退潮→启动(V形)：溢价≥0.5 且 跌停<5 且 情绪≥40。
- **各阶段交易 stance**：

| 阶段 | 自动买入 | 说明 |
|---|---|---|
| 冰点 | 禁止 | 空仓观望，现金为王 |
| 启动 | 只买推荐 | 精选试错，小仓位 |
| 发酵 | 放开 | 积极进攻，接力打板 |
| 高潮 | 放开 | 逢高减仓，不追高 |
| 退潮 | 禁止 | 防守为主，持仓逢高减仓 |

盘中加载前一交易日阶段，数据陈旧（未达上一交易日）则告警。

---

## 十二、看门狗与自愈

- 主循环每轮更新心跳；心跳超过 `WATCHDOG_STALL_SECONDS=120s` 未更新 → 疑似卡死（数据源挂起/网络阻塞）。
- 看门狗线程每 `WATCHDOG_CHECK_SECONDS=20s` 检查一次，卡死推送一次告警并置位，恢复后复位可再次告警。
- `WATCHDOG_AUTO_RESTART=True`：检测到卡死自动拉起新 main.py 进程并退出当前进程（自愈）。
- 自动重启冷却 `WATCHDOG_RESTART_COOLDOWN_MINUTES=10`：冷却期内再次卡死 → 停止自动重启（防循环），交人工。
- 重启后通过 `logs/.watchdog_restart` 标记感知"是自动重启"，推送恢复确认。
- 其他自愈：08:30 盘前简报缺失时盘中补发一次。

---

## 十三、数据源超时与熔断

- **socket 超时**：所有数据抓取包裹 `socket_timeout(12s)`（`data/core.py`），akshare 内部 requests 未传 timeout，源挂起时 12s 超时按源失败降级——防止主循环永久阻塞（历史 48 分钟停摆 bug 的修复）。
- **当日源熔断**：某源当日异常达 `SOURCE_FAIL_CIRCUIT_LIMIT=3` 次后熔断，当天不再调用该源（次日重置）。
- **熔断状态跨重启持久化**：源熔断状态落盘 `logs/.source_circuit.json`，看门狗自动重启后的新进程当天恢复"已熔断源"，直接走备用源、不再重打被限流源（切断"重启→重打→再熔断→再重启"循环）。日线 ETL 启动时同样加载。
- **东财全市场行情多主机轮换**（`_fetch_spot_eastmoney`）：东财 clist 端点按请求频率限流（实测连续 ~8 次请求即连接被 RemoteDisconnected 重置），akshare 默认 pz=100 分页拉全市场需 ~59 次请求必被断。改为临时补丁 akshare 分页层：① 加大 `pz=2000` 减少分页次数；② 逐页轮换 `82/92/push2/7/30.push2` 主机。解析仍交给 akshare（列口径一致）。
- **腾讯/新浪并行抓页**（`SPOT_FETCH_PARALLEL`，默认 True）：腾讯/新浪全市场行情 akshare 串行分页慢（腾讯 28 页 ~30s、新浪 74 页 ~60s），改并行分页抓取（实测腾讯 8.7s、新浪 3.4s）。页数上限实测：**腾讯 `count` 硬上限 200**（加大返回空）、**新浪 `num` 上限 100**（已用满）。
- **并行反爬自动切串行**：并行抓取连续失败 2 次（异常或返回空，疑似触发反爬/限流）→ 自动熔断该源并行、切回 akshare 串行，600s 冷却后重试并行；成功即清零计数。`SPOT_FETCH_PARALLEL=false` 恒走串行。新浪反爬严格，被临时封 IP 时系统会自动切串行，无需人工干预。
- **抓取重试**：`FETCH_RETRY_COUNT=3`、`FETCH_RETRY_DELAY=2s`（指数退避）。
- **涨停/炸板池失败退避**：刷新失败后 `POOL_CACHE_FAIL_BACKOFF_SECONDS=300s` 再试。
- **日线 ETL**：`run_incremental` 拉近 30 天到今天的整个区间，断点续传跳过已完整覆盖 code；`run_serial` 串行低 QPS 补拉多轮收敛；进程内互斥锁 `_etl_lock` 防跨天重叠；失败 `logger.error` + Bark 系统告警。

---

## 十四、阈值速查表

> 完整清单见 `config/settings.py`。此处列监控/风控核心项，`default` 为未在 `.env` 覆盖时的值。

| 配置项 | 默认 | 含义 |
|---|---|---|
| MONITOR_INTERVAL_SECONDS | 15 | 盘中轮询间隔(秒) |
| MONITOR_POOL_CACHE_SECONDS | 60 | 涨停/炸板池缓存间隔(秒) |
| MONITOR_CYCLE_BUDGET_SECONDS | 90 | 单轮周期时间预算(秒)，超预算跳过非关键步骤防看门狗重启 |
| SPOT_FETCH_PARALLEL | true | 腾讯/新浪行情并行分页抓取(腾讯页上限200/新浪100) |
| VOL_BURST_THRESHOLD | 3.0 | 点火异动量比门槛 |
| PRICE_BURST_THRESHOLD | 3.0 | 点火异动涨幅下限(%) |
| PRICE_BURST_MAX / _20CM | 9.5 / 19.5 | 点火异动涨幅上限（主板/双创，已涨停不算） |
| MONITOR_NEAR_LIMIT_RATIO | 0.84 | 逼近封板区间 = 涨停线×比值 |
| NEAR_LIMIT_VOL_RATIO | 5.0 | 逼近封板量比下限 |
| RALLY_VOL_RATIO | 3.0 | 低开猛拉/振幅放量量比下限 |
| RALLY_STRENGTH_MIN | 0.8 | 低开猛拉拉升强度下限 |
| RALLY_DENOM_MIN_RATIO | 0.01 | 低开猛拉分母保护：价差低于昨收×此比例时按昨收×比例计 |
| RALLY_MIN_PCT | 2.0 | 低开猛拉最小拉升幅度(%昨收) |
| AI_BUY_NEAR_LIMIT_FILL_LIMIT | true | 逼近封板/高位放量买入按涨停价撮合 |
| LOW_OPEN_DEV | 0.98 | 低开判定：开盘/昨收上限 |
| AMPLITUDE_SIGNAL_MIN | 7.0 | 振幅放量振幅下限(%) |
| AMPLITUDE_CHANGE_MIN | 3.0 | 振幅放量涨幅下限(%) |
| MAIN_BOARD_LIMIT_PCT / GEM_STAR_LIMIT_PCT | 9.8 / 19.8 | 涨停线判定(%) |
| TAIL_GAME_CHANGE_MIN / MAX | 2.0 / 5.0 | 尾盘博弈涨幅区间(%) |
| TAIL_GAME_VOL_RATIO | 3.0 | 尾盘博弈量比下限 |
| TAIL_GAME_SHORT_UPPER_RATIO | 0.3 | 尾盘博弈上影线占比上限 |
| TAIL_GAME_OPEN_GAP_PCT | 2.0 | 尾盘次日高开判定(%) |
| TAIL_GAME_TAKE_RATIO | 0.5 | 尾盘回测高开兑现比例 |
| TAIL_GAME_TRAIL_PULLBACK_PCT | 3.0 | 尾盘实盘高开动态止盈回落阈值(%)，10:30强平兜底 |
| TAIL_GAME_MORNING_HIGH_CAP_PCT | 5.0 | 尾盘回测窗口最高价封顶(开盘×1+%) |
| TAIL_MAX_DAILY_BUYS / TAIL_MAX_POSITIONS | 2 / 2 | 尾盘当日次数/持仓上限 |
| TAIL_LLM_PER_CYCLE | 1 | 尾盘每轮 LLM 确认次数 |
| SECOND_WAVE_RETREAT_MIN / MAX | 0.30 / 0.50 | 二波回撤区间 |
| SECOND_WAVE_LOOKBACK_DAYS | 30 | 二波追溯龙头天数 |
| SECOND_WAVE_CHANGE_MIN | 3.0 | 二波止跌信号涨幅下限(%) |
| SW_MAX_DAILY_BUYS / SW_MAX_POSITIONS | 2 / 2 | 二波当日次数/持仓上限 |
| SW_HOLD_DAYS | 5 | 二波 N 天未创新高离场 |
| MAX_AI_POSITIONS | 5 | AI 持仓上限 |
| MAX_DAILY_BUYS | 3 | AI 当日买入次数上限 |
| MAX_AI_SECTOR_POSITIONS | 2 | 同板块持仓上限 |
| DAILY_LOSS_CIRCUIT_BREAKER | -5.0 | AI 持仓当日平均亏损熔断(%) |
| INDEX_DROP_CIRCUIT_BREAKER | -2.0 | 大盘均涨幅熔断(%) |
| ABSOLUTE_STOP_LOSS_PCT | -7.0 | 绝对止损线(%) |
| TIME_STOP_LOSS_DAYS | 3 | 时间止损天数 |
| TAKE_PROFIT_WARN_PCT / CRITICAL_PCT | 15 / 20 | 止盈提醒/强止盈线(%) |
| TAKE_PROFIT_HIGH_PULLBACK_PCT | 5.0 | 强止盈自当日高点最低回落(%) |
| REC_FADE_MAX | 2.0 | 相对开盘回落走弱上限(%) |
| MA5_FALLBACK_RATIO | 0.97 | MA5 获取失败兜底 = 现价×0.97 |
| PATTERN_CHECK_MIN_AMOUNT | 5.0 | 分时形态检测最低成交额(亿) |
| PATTERN_CHECK_CACHE_SECONDS | 300 | 分时形态缓存 TTL(秒) |
| ZHABAN_ALERT_CHANGE / VOL_RATIO | 7.0 / 2.0 | 炸板预警涨幅/量比 |
| SECTOR_LINKAGE_MIN_COUNT / ACCEL_DELTA | 3 / 2 | 板块联动涨停家数/加速增量 |
| EMOTION_TOP_MAX_LBC / ZHABAN_RATE / SENTIMENT_MIN | 8 / 35 / 75 | 情绪到顶预警条件 |
| FUND_INFLOW_MIN / CAP_RATIO | 2000 / 0.0005 | 大单抱团绝对底线(万)/市值比例 |
| LLM_BUY_CONFIRM_PER_CYCLE | 1 | 每轮 LLM 买入确认次数 |
| LLM_SELL_HOLD_COOLDOWN_SECONDS | 1800 | 卖出 LLM 判持有后冷却(秒) |
| LLM_SELL_COOLDOWN_BREAK_PCT | 3.0 | 冷却期内急跌≥此值打破冷却重评估(%) |
| AI_BUY_SLIPPAGE_PCT / HOT_PCT | 0.3 / 0.2 | 买入滑点 / 高位放量额外滑点(%) |
| AI_SELL_SLIPPAGE_PCT | 0.3 | 卖出滑点(%) |
| WATCHDOG_STALL_SECONDS / CHECK_SECONDS | 120 / 20 | 看门狗卡死判定/检查间隔(秒) |
| WATCHDOG_AUTO_RESTART / COOLDOWN_MINUTES | True / 10 | 自动重启开关/冷却(分钟) |
| SOURCE_FAIL_CIRCUIT_LIMIT | 3 | 源当日熔断次数 |
| REGULATORY_MONITOR_ENABLED | True | 监管异动计算开关 |
| REGULATORY_GATE_ENABLED | True | 盘中买入监管异动闸门(一级/二级拦截) |
| MAIN_BOARD_3D_DEV_LIMIT / GEM_3D / STAR_3D | 20 / 30 / 30 | 3日偏离度红线(%)（北交所硬编码45） |
| REGULATORY_10D_LIMIT | 100.0 | 10日严重异动红线(%) |
| MAX_YIDONG_COUNT_10D | 4 | 10日内异动次数上限 |
| CORE_POOL_MIN_AMOUNT | 12.0 | 中军成交额门槛(亿) |
| CORE_POOL_TOP_AMOUNT / TOP_MARKET_CAP | 3 / 5 | 中军成交额/市值 TopN |
| CORE_POOL_MIN_BETA | 0.8 | 中军相关性阈值 |
| CONCEPT_GATE_ENABLED | True | 盘中概念否决开关 |
| CONCEPT_MAINLINE_SCORE_THRESHOLD | 0.5 | 概念主线分阈值 |
| EXCLUDE_STAR_MARKET / BSE / ST | True | 过滤科创板/北交所/ST |
